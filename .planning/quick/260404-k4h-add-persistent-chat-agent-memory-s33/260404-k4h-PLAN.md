---
phase: quick
plan: 260404-k4h
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - lib/agent/memory.ts
  - lib/agent/tools/remember.ts
  - lib/agent/tools/index.ts
  - app/api/chat/route.ts
  - app/api/agent/memory/route.ts
  - lib/agent/jobs/refresh-memory.ts
  - components/SettingsClient.tsx
autonomous: true
requirements: [S33]

must_haves:
  truths:
    - "Agent remembers explicit preferences across chat sessions"
    - "Will can say 'remember that...' and the agent persists it via tool"
    - "Memory context appears in system prompt (< 500 tokens)"
    - "Weekly refresh job updates pattern keys from trip/gear history"
    - "Clear agent memory button works in Settings"
  artifacts:
    - path: "lib/agent/tools/remember.ts"
      provides: "remember_this tool for explicit preference storage"
    - path: "lib/agent/jobs/refresh-memory.ts"
      provides: "Weekly scheduled job to refresh camping_patterns and gear_preferences"
    - path: "app/api/agent/memory/route.ts"
      provides: "GET/DELETE endpoints for memory management"
  key_links:
    - from: "app/api/chat/route.ts"
      to: "lib/agent/memory.ts"
      via: "loadMemoryContext() injected into system prompt"
      pattern: "loadMemoryContext"
    - from: "lib/agent/tools/index.ts"
      to: "lib/agent/tools/remember.ts"
      via: "AGENT_TOOLS array + executeAgentTool dispatcher"
      pattern: "remember_this"
---

<objective>
Complete the S33 Chat Agent Memory feature. The AgentMemory model already exists in schema but is missing `summary` and `createdAt` fields. The `memory.ts` module exists with `buildContextWindow()` and `extractAndSaveMemory()` but is missing `loadMemoryContext()` and `upsertMemory()`. This plan fills all gaps: schema migration, missing functions, remember_this tool, refresh job, API route, and Settings clear button.

Purpose: Give the chat agent persistent memory between conversations so it references Will's camping patterns and explicit preferences.
Output: Complete S33 feature — agent remembers across sessions, has a remember_this tool, weekly refresh job, and Settings clear button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@prisma/schema.prisma (AgentMemory model at ~line 373)
@lib/agent/memory.ts (existing buildContextWindow + extractAndSaveMemory)
@lib/agent/tools/index.ts (AGENT_TOOLS array + executeAgentTool dispatcher)
@app/api/chat/route.ts (uses buildContextWindow, system: CAMPING_EXPERT_SYSTEM_PROMPT)
@lib/agent/system-prompt.ts (CAMPING_EXPERT_SYSTEM_PROMPT static + buildSystemPrompt dynamic)
@lib/agent/jobs/weekly-briefing.ts (reference for job handler pattern)
@components/SettingsClient.tsx (add clear memory button)
</context>

<interfaces>
<!-- Existing contracts the executor needs -->

From lib/agent/memory.ts:
```typescript
export async function buildContextWindow(
  conversationId: string | null,
  pageContext?: string
): Promise<Array<{ role: 'user' | 'assistant'; content: string }>>;

export async function extractAndSaveMemory(assistantMessage: string, userMessage: string): Promise<void>;
```

From lib/agent/tools/index.ts:
```typescript
export const AGENT_TOOLS: Tool[];  // Array of Anthropic Tool definitions
export async function executeAgentTool(name: string, input: ToolInput): Promise<string>;
```

From lib/agent/system-prompt.ts:
```typescript
export const CAMPING_EXPERT_SYSTEM_PROMPT: string;  // Static prompt used in chat route
export function buildSystemPrompt(ctx: SystemPromptContext): string;  // Dynamic prompt with memories
```

From app/api/chat/route.ts (line 96):
```typescript
system: CAMPING_EXPERT_SYSTEM_PROMPT,  // This is where memory context needs injection
```

From prisma/schema.prisma (current AgentMemory — missing summary + createdAt):
```prisma
model AgentMemory {
  id        String   @id @default(cuid())
  key       String   @unique
  value     String
  updatedAt DateTime @updatedAt
  @@index([key])
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Schema migration + memory module functions + remember_this tool + API route</name>
  <files>
    prisma/schema.prisma,
    lib/agent/memory.ts,
    lib/agent/tools/remember.ts,
    lib/agent/tools/index.ts,
    app/api/agent/memory/route.ts
  </files>
  <action>
  1. **Schema migration** (manual SQL since non-interactive env):
     - Add `summary` (String, default '') and `createdAt` (DateTime, default CURRENT_TIMESTAMP) columns to AgentMemory in schema.prisma.
     - Create a new migration SQL file in prisma/migrations/ with format `YYYYMMDDHHMMSS_add_agent_memory_fields/migration.sql`.
     - SQL: `ALTER TABLE "AgentMemory" ADD COLUMN "summary" TEXT NOT NULL DEFAULT ''; ALTER TABLE "AgentMemory" ADD COLUMN "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP;`
     - Run `npx prisma generate` to regenerate client.

  2. **Add loadMemoryContext() and upsertMemory() to lib/agent/memory.ts:**
     - `loadMemoryContext(): Promise<string>` — fetches all AgentMemory entries, formats as a compact block: one line per memory `- {key}: {summary || value}` (prefer summary if non-empty, fall back to value truncated to 100 chars). Return empty string if no memories. Total output must stay under 500 tokens (~375 words). If over, truncate oldest entries.
     - `upsertMemory(key: string, value: string, summary: string): Promise<void>` — wraps prisma.agentMemory.upsert by key. For key `explicit_preferences`: append to existing value (parse existing as JSON array, push new entry, stringify) rather than replacing. All other keys: overwrite.
     - Update `extractAndSaveMemory()` to also pass a summary string when upserting (generate a 1-line summary from the key+value).

  3. **Create lib/agent/tools/remember.ts:**
     - Export `rememberThisTool` (Tool definition): name `remember_this`, description "Save a user preference or fact for future sessions", input_schema with `preference` (string, required — the thing to remember).
     - Export `executeRememberThis({ preference })`: calls `upsertMemory('explicit_preferences', preference, preference.slice(0, 80))`. Returns confirmation string.

  4. **Register in lib/agent/tools/index.ts:**
     - Import `rememberThisTool` and `executeRememberThis` from `./remember`.
     - Add `rememberThisTool` to `AGENT_TOOLS` array.
     - Add `case 'remember_this'` to `executeAgentTool` switch.

  5. **Create app/api/agent/memory/route.ts:**
     - GET: return all AgentMemory entries as JSON array.
     - DELETE: accept `{ key?: string }` body. If key provided, delete that entry. If no key, delete ALL entries (used by Settings clear button). Return `{ success: true }`.
     - Standard try-catch error handling pattern.
  </action>
  <verify>
    <automated>cd /Users/willis/Camping\ Manager/.claude/worktrees/agitated-kare && npx prisma generate && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - AgentMemory schema has summary + createdAt fields
    - loadMemoryContext() returns compact string block
    - upsertMemory() handles append-only for explicit_preferences
    - remember_this tool registered in AGENT_TOOLS
    - GET /api/agent/memory returns memories, DELETE clears them
    - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: System prompt injection + refresh job + Settings clear button</name>
  <files>
    app/api/chat/route.ts,
    lib/agent/jobs/refresh-memory.ts,
    components/SettingsClient.tsx
  </files>
  <action>
  1. **Inject memory into system prompt in app/api/chat/route.ts:**
     - Import `loadMemoryContext` from `@/lib/agent/memory`.
     - Before the BetaToolRunner call, call `const memoryBlock = await loadMemoryContext()`.
     - Replace the static `system: CAMPING_EXPERT_SYSTEM_PROMPT` with `system: CAMPING_EXPERT_SYSTEM_PROMPT + (memoryBlock ? '\n\nYOUR MEMORY (preferences and patterns you have learned about this user):\n' + memoryBlock : '')`.
     - The existing `buildContextWindow()` still handles conversation history, but remove the duplicate memory injection from it — the memory block in buildContextWindow (lines 24-38) now overlaps with the system prompt injection. Refactor: remove the memory-loading logic from buildContextWindow so memories only appear once (in the system prompt). Keep the pageContext injection in buildContextWindow as-is.

  2. **Create lib/agent/jobs/refresh-memory.ts:**
     - Export `async function refreshMemory(): Promise<string>` (same pattern as other job handlers in lib/agent/jobs/).
     - Query: all trips with locations, last 10 TripFeedback entries, last 10 Meal feedback entries.
     - Build a summary object:
       - `camping_patterns`: trip count, avg duration, most-visited locations (top 3), preferred seasons (group trips by month), avg drive distance.
       - `gear_preferences`: items marked "used" most often, items marked "didn't_need" 2+ times, items marked "forgot".
     - Upsert each key via `upsertMemory()` with a compact JSON value and a human-readable summary.
     - Return a status string describing what was refreshed.
     - This job will be registered in the scheduler by adding it to the jobs registry. Check how existing jobs in lib/agent/jobs/ are registered (likely in app/api/agent/jobs/route.ts or a jobs index) and follow the same pattern. The job should run weekly.

  3. **Add "Clear Agent Memory" button to components/SettingsClient.tsx:**
     - Add a new section after existing settings content: "Agent Memory" with a brief description "The chat agent remembers your preferences and camping patterns between conversations."
     - Add a "Clear All Memory" button (red/destructive styling).
     - On click, fetch DELETE /api/agent/memory with no body (clears all).
     - Show success/error state inline (no alert()).
     - Use the same button/card styling patterns as the rest of SettingsClient.
  </action>
  <verify>
    <automated>cd /Users/willis/Camping\ Manager/.claude/worktrees/agitated-kare && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - Chat system prompt includes memory block from loadMemoryContext()
    - No duplicate memory injection (removed from buildContextWindow)
    - refresh-memory job queries trip/gear data and upserts pattern keys
    - Settings page has "Clear Agent Memory" button that calls DELETE /api/agent/memory
    - TypeScript compiles clean
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `npx prisma generate` succeeds with updated schema
- Manual check: review system prompt injection in chat route to confirm memory appears once (not duplicated in both system prompt and messages array)
- Verify remember_this is in AGENT_TOOLS array
- Verify refresh-memory.ts exports follow the same pattern as other job handlers
</verification>

<success_criteria>
- Agent has persistent memory between chat sessions via AgentMemory table
- Will can say "remember that..." and agent calls remember_this tool
- Memory context injected into system prompt as compact block (< 500 tokens)
- Weekly refresh job exists to update camping_patterns and gear_preferences
- Settings page has Clear Agent Memory button
- Build passes, no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/quick/260404-k4h-add-persistent-chat-agent-memory-s33/260404-k4h-SUMMARY.md`
</output>
