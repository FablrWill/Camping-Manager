---
phase: 34-meal-planning-core
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/meal-plan-route.test.ts
  - tests/meal-regenerate-route.test.ts
  - tests/meal-plan-schema.test.ts
autonomous: true
requirements:
  - MEAL-01
  - MEAL-02
  - MEAL-03
  - MEAL-04
  - MEAL-05
  - MEAL-06
  - MEAL-07
must_haves:
  truths:
    - "All 3 test stub files exist and are loadable by vitest"
    - "All tests are marked .todo so they fail as pending, not as errors"
    - "Test stubs describe the behaviors that must pass after implementation"
  artifacts:
    - path: "tests/meal-plan-route.test.ts"
      provides: "Failing stubs for MEAL-01 through MEAL-04 (generate, GET, DELETE routes)"
      min_lines: 40
    - path: "tests/meal-regenerate-route.test.ts"
      provides: "Failing stubs for MEAL-05 and MEAL-06 (per-meal regenerate + 404 ownership check)"
      min_lines: 25
    - path: "tests/meal-plan-schema.test.ts"
      provides: "Failing stubs for MEAL-07 (SingleMealSchema + NormalizedIngredientSchema Zod validation)"
      min_lines: 20
  key_links:
    - from: "tests/meal-plan-route.test.ts"
      to: "app/api/trips/[id]/meal-plan/generate/route.ts"
      via: "import POST from route (after Plan 02 creates it)"
      pattern: "meal-plan/generate"
    - from: "tests/meal-regenerate-route.test.ts"
      to: "app/api/trips/[id]/meal-plan/meals/[mealId]/route.ts"
      via: "import PATCH from route (after Plan 02 creates it)"
      pattern: "meals.*mealId"
    - from: "tests/meal-plan-schema.test.ts"
      to: "lib/parse-claude.ts"
      via: "import SingleMealSchema, NormalizedIngredientSchema"
      pattern: "SingleMealSchema"
---

<objective>
Create Wave 0 test stub files for all meal planning routes and schemas. Stubs use `.todo` markers so they are pending (not erroring) until implementation fills them in.

Purpose: Nyquist compliance — every implementation task in Plans 01-03 must have a test file that existed before the implementation was written. These stubs define the behavior contract.
Output: 3 test files in tests/ directory, all loadable by vitest, all tests marked as .todo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-meal-planning-core/34-CONTEXT.md
@.planning/phases/34-meal-planning-core/34-RESEARCH.md
@.planning/phases/34-meal-planning-core/34-VALIDATION.md

<interfaces>
<!-- Test pattern from tests/vehicle-checklist-route.test.ts -->
import { describe, it, expect, vi, beforeEach } from 'vitest'

vi.mock('@/lib/db', () => ({
  prisma: {
    trip: { findUnique: vi.fn(), update: vi.fn() },
    mealPlan: { findUnique: vi.fn(), upsert: vi.fn(), delete: vi.fn() },
    meal: { findUnique: vi.fn(), createMany: vi.fn(), deleteMany: vi.fn(), update: vi.fn(), delete: vi.fn() },
    gearItem: { findMany: vi.fn() },
    $transaction: vi.fn(),
  },
}))

vi.mock('@/lib/claude', () => ({
  generateMealPlan: vi.fn(),
  regenerateMeal: vi.fn(),
}))

<!-- Schema test pattern from tests/vehicle-checklist-schema.test.ts -->
import { describe, it, expect } from 'vitest'
import { SingleMealSchema, NormalizedIngredientSchema } from '@/lib/parse-claude'

<!-- Route files created in Plan 02 (will not exist when Wave 0 runs) -->
app/api/trips/[id]/meal-plan/route.ts — GET + DELETE
app/api/trips/[id]/meal-plan/generate/route.ts — POST
app/api/trips/[id]/meal-plan/meals/[mealId]/route.ts — PATCH + DELETE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 3 test stub files with .todo markers</name>
  <files>tests/meal-plan-route.test.ts, tests/meal-regenerate-route.test.ts, tests/meal-plan-schema.test.ts</files>
  <read_first>
    - tests/vehicle-checklist-route.test.ts (full file — mock pattern, makeRequest helper, params as Promise)
    - tests/vehicle-checklist-schema.test.ts (full file — Zod schema test pattern)
  </read_first>
  <action>
Create 3 test files. All tests use `it.todo()` so they show as pending in vitest output (not failing, not passing). The mocks and imports at the top level are real — only the test bodies are stubs.

**IMPORTANT:** Do NOT import route handlers yet — the route files do not exist until Plan 02. Use comments indicating which imports to add after Plan 02 creates the routes. The mock setup (`vi.mock`) IS needed now so the files are structurally valid.

**File 1: `tests/meal-plan-route.test.ts`**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Mock db and claude before any imports
vi.mock('@/lib/db', () => ({
  prisma: {
    trip: { findUnique: vi.fn(), update: vi.fn() },
    mealPlan: { findUnique: vi.fn(), upsert: vi.fn(), delete: vi.fn() },
    meal: { findUnique: vi.fn(), createMany: vi.fn(), deleteMany: vi.fn(), update: vi.fn(), delete: vi.fn() },
    gearItem: { findMany: vi.fn() },
    $transaction: vi.fn(),
  },
}))

vi.mock('@/lib/claude', () => ({
  generateMealPlan: vi.fn(),
}))

vi.mock('@/lib/weather', () => ({
  fetchWeather: vi.fn(),
}))

// Route imports — add after Plan 02 creates the files:
// import { POST } from '@/app/api/trips/[id]/meal-plan/generate/route'
// import { GET, DELETE } from '@/app/api/trips/[id]/meal-plan/route'

describe('POST /api/trips/:id/meal-plan/generate', () => {
  // MEAL-01: POST /generate creates MealPlan + Meal rows, updates Trip.mealPlanGeneratedAt
  it.todo('creates MealPlan and Meal rows from Claude response')
  it.todo('updates Trip.mealPlanGeneratedAt after successful generation')
  it.todo('returns 404 when trip not found')
  it.todo('returns 400 when trip has no start or end date')

  // MEAL-02: POST /generate passes bringingDog to generateMealPlan
  it.todo('passes bringingDog=true to generateMealPlan when trip.bringingDog is true')
  it.todo('passes bringingDog=false to generateMealPlan when trip.bringingDog is false')
})

describe('GET /api/trips/:id/meal-plan', () => {
  // MEAL-03: GET /meal-plan returns plan with meals array
  it.todo('returns mealPlan with nested meals array when plan exists')
  it.todo('returns { mealPlan: null } when no plan exists')
  it.todo('parses ingredients JSON string into array of objects')
})

describe('DELETE /api/trips/:id/meal-plan', () => {
  // MEAL-04: DELETE /meal-plan clears MealPlan + cascades Meal rows
  it.todo('deletes MealPlan and returns { success: true }')
  it.todo('returns { success: true } even when no plan exists')
})
```

**File 2: `tests/meal-regenerate-route.test.ts`**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

vi.mock('@/lib/db', () => ({
  prisma: {
    trip: { findUnique: vi.fn() },
    meal: { findUnique: vi.fn(), update: vi.fn(), delete: vi.fn() },
    gearItem: { findMany: vi.fn() },
  },
}))

vi.mock('@/lib/claude', () => ({
  regenerateMeal: vi.fn(),
}))

vi.mock('@/lib/weather', () => ({
  fetchWeather: vi.fn(),
}))

// Route import — add after Plan 02 creates the file:
// import { PATCH, DELETE } from '@/app/api/trips/[id]/meal-plan/meals/[mealId]/route'

describe('PATCH /api/trips/:id/meal-plan/meals/:mealId', () => {
  // MEAL-05: PATCH calls regenerateMeal and updates the Meal row
  it.todo('calls regenerateMeal with correct context and updates meal row')
  it.todo('returns updated meal with parsed ingredients')

  // MEAL-06: PATCH returns 404 when mealId does not belong to trip
  it.todo('returns 404 when meal not found')
  it.todo('returns 404 when meal belongs to a different trip')
})

describe('DELETE /api/trips/:id/meal-plan/meals/:mealId', () => {
  it.todo('deletes a single meal and returns { success: true }')
  it.todo('returns 404 when meal does not belong to trip')
})
```

**File 3: `tests/meal-plan-schema.test.ts`**

```typescript
import { describe, it, expect } from 'vitest'
// Schema imports — add after Plan 01 creates them:
// import { NormalizedIngredientSchema, SingleMealSchema, NormalizedMealPlanResultSchema } from '@/lib/parse-claude'

describe('NormalizedIngredientSchema', () => {
  // MEAL-07: validates ingredient shape
  it.todo('accepts valid ingredient with item, quantity, and unit')
  it.todo('defaults unit to empty string when omitted')
  it.todo('rejects ingredient missing item field')
  it.todo('rejects ingredient missing quantity field')
})

describe('SingleMealSchema', () => {
  // MEAL-07: validates single meal shape for regenerateMeal output
  it.todo('accepts valid meal with all fields')
  it.todo('accepts meal with only required fields (name + ingredients)')
  it.todo('rejects meal without name')
  it.todo('rejects meal with string[] ingredients instead of object[]')
})

describe('NormalizedMealPlanResultSchema', () => {
  // MEAL-07: validates full meal plan response shape
  it.todo('accepts valid meal plan with days array')
  it.todo('rejects empty object')
})
```

Each file must:
- Have valid TypeScript syntax (loadable by vitest)
- Use `it.todo()` for all test cases (shows as "pending" in vitest, does not fail)
- Include comment annotations linking each describe/it block to its MEAL-XX requirement
- Have vi.mock() blocks at the top for db and claude (structurally valid even though route imports are commented out)
  </action>
  <verify>
    <automated>cd "/Users/willis/Camping Manager/.claude/worktrees/vibrant-lumiere" && npx vitest run tests/meal-plan-route.test.ts tests/meal-regenerate-route.test.ts tests/meal-plan-schema.test.ts 2>&1 | tail -15</automated>
  </verify>
  <done>All 3 test stub files exist in tests/, are loadable by vitest, and show all tests as "todo/pending". No test errors. Each stub describes the behavior contract for MEAL-01 through MEAL-07.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/meal-plan-route.test.ts tests/meal-regenerate-route.test.ts tests/meal-plan-schema.test.ts` exits 0 with all tests shown as "todo"
- `wc -l tests/meal-plan-route.test.ts` shows 40+ lines
- `wc -l tests/meal-regenerate-route.test.ts` shows 25+ lines
- `wc -l tests/meal-plan-schema.test.ts` shows 20+ lines
</verification>

<success_criteria>
1. All 3 test files exist and are syntactically valid TypeScript
2. vitest can load all 3 files without errors
3. All tests show as "todo/pending" (not failing, not passing)
4. Test descriptions cover MEAL-01 through MEAL-07 requirements
5. Mock patterns follow existing vehicle-checklist-route.test.ts convention
</success_criteria>

<output>
After completion, create `.planning/phases/34-meal-planning-core/34-00-SUMMARY.md`
</output>
