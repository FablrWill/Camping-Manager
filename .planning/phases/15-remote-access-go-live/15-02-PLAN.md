---
phase: 15-remote-access-go-live
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: []
autonomous: false
requirements: [ACCESS-02, ACCESS-05]

must_haves:
  truths:
    - "Tailscale is installed and connected on Will's iPhone"
    - "The app loads over HTTPS from Will's phone via Tailscale"
    - "The app is installable as a PWA from Safari on Will's phone"
    - "Offline mode works after PWA install over the Tailscale connection"
  artifacts: []
  key_links:
    - from: "iPhone Tailscale app"
      to: "Mac mini Tailscale node"
      via: "WireGuard encrypted tunnel"
      pattern: "tailscale status shows both devices on same tailnet"
    - from: "Safari on iPhone"
      to: "https://[hostname].ts.net"
      via: "MagicDNS resolution over Tailscale"
      pattern: "Page loads with padlock icon in Safari"
---

<objective>
Install Tailscale on Will's iPhone, verify the full remote access flow, and complete the PWA install from his phone — the final go-live verification that proves Outland OS is usable from anywhere.

Purpose: This is the end-to-end proof that the entire v1.2 "Ship It" milestone delivers. Will can use Outland OS from his phone over an encrypted private network.
Output: Working PWA on Will's phone accessing Outland OS on the Mac mini via Tailscale
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-remote-access-go-live/15-CONTEXT.md
@.planning/phases/15-remote-access-go-live/15-RESEARCH.md
@.planning/phases/15-remote-access-go-live/15-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install Tailscale on iPhone and connect to tailnet</name>
  <read_first>
    .planning/phases/15-remote-access-go-live/15-CONTEXT.md
    .planning/phases/15-remote-access-go-live/15-RESEARCH.md
  </read_first>
  <action>
    Will must install Tailscale on his iPhone from the App Store and log in with the SAME Tailscale account used on the Mac mini. This step cannot be automated — it requires the physical iPhone.

    Steps for Will:
    1. Open App Store on iPhone
    2. Search "Tailscale" and install the Tailscale app
    3. Open the app and sign in with the same account used on the Mac mini
    4. Toggle the VPN on — iOS will prompt to allow VPN configuration (tap Allow)
    5. Verify: the Tailscale app shows a green dot (connected) and the Mac mini appears in the device list

    Per D-04: iPhone Tailscale install is part of Phase 15 (ACCESS-02), and it is a manual step.

    IMPORTANT: Use the SAME Tailscale account as the Mac mini. Both devices must be on the same tailnet to see each other.
  </action>
  <how-to-verify>
    1. Tailscale app is installed on iPhone
    2. Tailscale app shows green dot (connected)
    3. Mac mini appears in the device list in the Tailscale iOS app
    4. On Mac mini, `tailscale status` shows both the Mac mini AND the iPhone as connected nodes
  </how-to-verify>
  <resume-signal>Confirm "Tailscale installed on iPhone, both devices visible on tailnet" or describe any issues</resume-signal>
  <acceptance_criteria>
    - Tailscale iOS app is installed and authenticated
    - `tailscale status` (on Mac mini) shows both Mac mini and iPhone as nodes
    - iPhone has VPN toggle enabled for Tailscale
  </acceptance_criteria>
  <done>Tailscale is installed on Will's iPhone and connected to the same tailnet as the Mac mini (ACCESS-02 complete)</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Go-live verification — full PWA flow from phone</name>
  <read_first>
    .planning/phases/15-remote-access-go-live/15-CONTEXT.md
    .planning/phases/15-remote-access-go-live/15-RESEARCH.md
  </read_first>
  <what-built>
    The complete remote access stack: Tailscale mesh VPN between Mac mini and iPhone, HTTPS via `tailscale serve`, app running at `https://[hostname].ts.net`.

    This task verifies D-07: "Done" = full PWA flow from Will's phone: connect via Tailscale MagicDNS, verify HTTPS works, install app to home screen, test that offline mode works over the connection.
  </what-built>
  <how-to-verify>
    Complete this checklist from Will's iPhone with Tailscale connected:

    **HTTPS Verification (ACCESS-04):**
    1. Open Safari on iPhone
    2. Navigate to `https://[hostname].ts.net` (use the MagicDNS hostname from Plan 01)
    3. VERIFY: Page loads without any certificate warnings
    4. VERIFY: Safari shows the padlock icon (secure connection)
    5. VERIFY: The Outland OS dashboard/home page renders correctly

    **PWA Install (ACCESS-05):**
    6. In Safari, tap the Share button (box with arrow)
    7. Tap "Add to Home Screen"
    8. VERIFY: App icon appears with the correct name ("Outland" or similar)
    9. Tap Add
    10. VERIFY: App icon appears on the iPhone home screen

    **PWA Launch Verification:**
    11. Close Safari completely
    12. Tap the Outland OS icon on the home screen
    13. VERIFY: App opens in standalone mode (no Safari navigation bar)
    14. VERIFY: App functions correctly — tap through gear, trips, spots pages

    **Offline Mode Verification:**
    15. With the app open and pages loaded, disconnect from Tailscale (toggle VPN off in Tailscale app)
    16. VERIFY: Previously visited pages still load from service worker cache
    17. Reconnect Tailscale when done

    If any step fails, describe which step and what you see.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which steps failed and what you observed</resume-signal>
  <acceptance_criteria>
    - Safari loads `https://[hostname].ts.net` with padlock icon (no cert warnings)
    - "Add to Home Screen" produces a working PWA icon on the iPhone
    - PWA opens in standalone mode (no browser chrome)
    - Offline mode works after disconnecting Tailscale VPN
  </acceptance_criteria>
  <done>Full PWA flow verified from Will's phone: HTTPS works, PWA installs, offline mode functions. Outland OS is live and accessible from anywhere via Tailscale. ACCESS-05 complete. Phase 15 DONE. v1.2 "Ship It" milestone COMPLETE.</done>
</task>

</tasks>

<verification>
All verification for Plan 02 is manual and performed by Will on his iPhone:
1. Tailscale iOS app connected (green dot)
2. `tailscale status` on Mac mini shows both devices
3. Safari loads app over HTTPS with padlock
4. PWA installs to home screen
5. PWA opens in standalone mode
6. Offline mode works after VPN disconnect
7. `npm test -- --run` on Mac mini confirms no regressions
</verification>

<success_criteria>
- Tailscale is installed and connected on Will's iPhone (same tailnet as Mac mini)
- The app loads correctly at `https://[hostname].ts.net` from Safari on iPhone
- PWA is installed on iPhone home screen and opens in standalone mode
- Offline cached pages load when Tailscale VPN is disconnected
- This is the final verification for v1.2 "Ship It" milestone
</success_criteria>

<output>
After completion, create `.planning/phases/15-remote-access-go-live/15-02-SUMMARY.md`
</output>
