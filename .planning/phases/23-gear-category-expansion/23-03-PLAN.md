---
phase: 23-gear-category-expansion
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - components/GearClient.tsx
  - components/GearForm.tsx
autonomous: true
requirements: [GEAR-CAT-03, GEAR-CAT-04, GEAR-CAT-07]

must_haves:
  truths:
    - "Gear page renders 15 categories as grouped filter chips in 4 visual groups"
    - "Group headers (Living, Utility, Tech/Power, Action) are visible above their chips"
    - "GearForm has modelNumber, connectivity, manualUrl input fields"
    - "GearClient has no local CATEGORIES constant — imports from shared module"
    - "GearForm imports category options from shared module instead of receiving as prop"
  artifacts:
    - path: "components/GearClient.tsx"
      provides: "Gear page with grouped filter chips"
      contains: "import.*CATEGORY_GROUPS.*from.*gear-categories"
    - path: "components/GearForm.tsx"
      provides: "Gear form with tech detail fields"
      contains: "modelNumber"
  key_links:
    - from: "components/GearClient.tsx"
      to: "lib/gear-categories.ts"
      via: "import CATEGORY_GROUPS, CATEGORIES, getCategoryEmoji, getCategoryLabel"
      pattern: "import.*CATEGORY_GROUPS.*from.*gear-categories"
    - from: "components/GearForm.tsx"
      to: "lib/gear-categories.ts"
      via: "import CATEGORIES"
      pattern: "import.*CATEGORIES.*from.*gear-categories"
---

<objective>
Replace GearClient local categories with grouped filter chip UI using the shared module, and add tech gear fields to GearForm.

Purpose: Give Will a visual gear page with 15 categories in 4 groups and the ability to track model numbers and connectivity on tech gear.
Output: Updated GearClient with grouped chips, updated GearForm with 3 new fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-gear-category-expansion/23-CONTEXT.md
@.planning/phases/23-gear-category-expansion/23-RESEARCH.md
@.planning/phases/23-gear-category-expansion/23-01-SUMMARY.md

<interfaces>
From lib/gear-categories.ts (created in Plan 01):
```typescript
export type CategoryValue = 'shelter' | 'sleep' | 'cook' | 'hydration' | 'clothing' | 'lighting' | 'tools' | 'safety' | 'furniture' | 'power' | 'electronics' | 'vehicle' | 'navigation' | 'hiking' | 'dog'
export interface Category { value: CategoryValue; label: string; emoji: string }
export interface CategoryGroup { name: string; categories: Category[] }
export const CATEGORY_GROUPS: CategoryGroup[]
export const CATEGORIES: Category[]
export const CATEGORY_EMOJI: Record<string, string>
export function getCategoryEmoji(category: string): string
export function getCategoryLabel(category: string): string
```

From components/GearClient.tsx (current state):
```typescript
interface GearItem {
  id: string; name: string; brand: string | null; category: string;
  description: string | null; condition: string | null; weight: number | null;
  photoUrl: string | null; storageLocation: string | null; isWishlist: boolean;
  purchaseUrl: string | null; price: number | null; notes: string | null;
  wattage: number | null; hoursPerDay: number | null; hasBattery: boolean;
  createdAt: string; updatedAt: string;
}
// Local CATEGORIES at lines 28-36 — must be replaced
// Local getCategoryEmoji at line 46 — must be replaced
// Local getCategoryLabel at line 50 — must be replaced
```

From components/GearForm.tsx (current state):
```typescript
interface GearFormProps {
  item: GearItem | null;
  categories: readonly CategoryOption[];
  conditions: readonly ConditionOption[];
  onSave: (data: Record<string, unknown>) => Promise<void>;
  onDelete?: () => void;
  onClose: () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace GearClient local categories with grouped filter chips</name>
  <files>components/GearClient.tsx</files>
  <read_first>
    - components/GearClient.tsx (FULL file — understand current CATEGORIES, getCategoryEmoji, getCategoryLabel, filter chip rendering, how categories prop is passed to GearForm)
    - lib/gear-categories.ts (verify CATEGORY_GROUPS, CATEGORIES, getCategoryEmoji, getCategoryLabel exports)
  </read_first>
  <action>
    1. Add import at top: `import { CATEGORY_GROUPS, CATEGORIES, getCategoryEmoji, getCategoryLabel } from '@/lib/gear-categories'`

    2. DELETE these local definitions:
       - `const CATEGORIES = [...]` (lines 28-36)
       - `function getCategoryEmoji(...)` (around line 46)
       - `function getCategoryLabel(...)` (around line 50)

    3. Add `modelNumber`, `connectivity`, and `manualUrl` to the local `GearItem` interface:
       ```typescript
       modelNumber: string | null
       connectivity: string | null
       manualUrl: string | null
       ```

    4. Replace the flat category filter chip rendering with grouped chips. Find where the filter chips are rendered (the section that maps over CATEGORIES to create clickable chips). Replace it with:

       ```tsx
       {CATEGORY_GROUPS.map((group) => (
         <div key={group.name} className="mb-2">
           <div className="text-xs font-medium text-gray-500 dark:text-gray-400 mb-1 uppercase tracking-wide">
             {group.name}
           </div>
           <div className="flex flex-wrap gap-1">
             {group.categories.map((cat) => (
               <button
                 key={cat.value}
                 onClick={() => setActiveCategory(activeCategory === cat.value ? null : cat.value)}
                 className={`px-2 py-1 rounded-full text-xs font-medium transition-colors ${
                   activeCategory === cat.value
                     ? 'bg-blue-500 text-white'
                     : 'bg-gray-100 dark:bg-gray-800 text-gray-700 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-700'
                 }`}
               >
                 {cat.emoji} {cat.label}
               </button>
             ))}
           </div>
         </div>
       ))}
       ```

       Preserve the existing `activeCategory` state variable and its `setActiveCategory` setter. The filtering logic should remain the same (filter items by `activeCategory` when it's not null).

    5. Where GearForm is rendered and `categories={CATEGORIES}` is passed as a prop — KEEP passing it for now. The next task will update GearForm to import directly, but for atomicity, keep the prop passing working with the imported CATEGORIES array. The shared module's CATEGORIES array is the same shape (has `value`, `label`, `emoji`).

    6. Ensure the existing `CONDITIONS` constant is NOT removed (it's unrelated to this change).
  </action>
  <verify>
    <automated>cd "/Users/willis/Camping Manager/.claude/worktrees/sweet-zhukovsky" && grep -n "CATEGORY_GROUPS" components/GearClient.tsx | head -5 && echo "---" && grep -c "const CATEGORIES" components/GearClient.tsx && echo "---" && grep -n "modelNumber" components/GearClient.tsx | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `components/GearClient.tsx` contains `import { CATEGORY_GROUPS, CATEGORIES, getCategoryEmoji, getCategoryLabel } from '@/lib/gear-categories'`
    - `components/GearClient.tsx` does NOT contain `const CATEGORIES = [` (local constant removed)
    - `components/GearClient.tsx` does NOT contain `function getCategoryEmoji` (local function removed)
    - `components/GearClient.tsx` does NOT contain `function getCategoryLabel` (local function removed)
    - `components/GearClient.tsx` contains `CATEGORY_GROUPS.map` (grouped rendering)
    - `components/GearClient.tsx` contains `group.name` (group header rendering)
    - `components/GearClient.tsx` GearItem interface contains `modelNumber: string | null`
    - `components/GearClient.tsx` GearItem interface contains `connectivity: string | null`
    - `components/GearClient.tsx` GearItem interface contains `manualUrl: string | null`
    - `components/GearClient.tsx` still contains `const CONDITIONS` (not accidentally removed)
  </acceptance_criteria>
  <done>GearClient renders 15 categories as grouped filter chips in 4 visual groups (Living, Utility, Tech/Power, Action). Local category constants removed, all imported from shared module.</done>
</task>

<task type="auto">
  <name>Task 2: Add tech gear fields to GearForm + import categories from shared module</name>
  <files>components/GearForm.tsx</files>
  <read_first>
    - components/GearForm.tsx (FULL file — understand current form fields, props interface, how categories prop is used, form submission handler)
    - lib/gear-categories.ts (verify CATEGORIES export)
    - components/GearClient.tsx (verify how GearForm is called — check if categories prop can be removed from callsite)
  </read_first>
  <action>
    1. Add import at top: `import { CATEGORIES } from '@/lib/gear-categories'`

    2. Update GearFormProps interface:
       - Remove `categories: readonly CategoryOption[]` from props
       - Remove the `CategoryOption` interface if it becomes unused
       - Keep `conditions` prop as-is (CONDITIONS stays in GearClient)

    3. Update the function signature to match (remove `categories` from destructured props)

    4. Replace all usages of the `categories` prop in the component body with the imported `CATEGORIES` array. They have the same shape (`{ value, label, emoji }`). Find where `categories.map(...)` is used in the form's category select/dropdown and replace with `CATEGORIES.map(...)`.

    5. Update the local GearItem interface to include the 3 new fields:
       ```typescript
       modelNumber: string | null
       connectivity: string | null
       manualUrl: string | null
       ```

    6. Add 3 new input fields to the form, placed after the existing `notes` or `storageLocation` field group (whichever is last). Use the same Tailwind styling as existing fields:

       ```tsx
       {/* Tech Details */}
       <div className="border-t pt-4 mt-4">
         <h3 className="text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">Tech Details (optional)</h3>

         <div className="space-y-3">
           <div>
             <label htmlFor="gear-modelNumber" className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
               Model Number
             </label>
             <input
               id="gear-modelNumber"
               name="modelNumber"
               type="text"
               defaultValue={item?.modelNumber ?? ''}
               placeholder="e.g. ESP32-WROOM-32"
               className="w-full rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 px-3 py-2 text-sm"
             />
           </div>

           <div>
             <label htmlFor="gear-connectivity" className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
               Connectivity
             </label>
             <input
               id="gear-connectivity"
               name="connectivity"
               type="text"
               defaultValue={item?.connectivity ?? ''}
               placeholder="e.g. WiFi, Bluetooth"
               className="w-full rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 px-3 py-2 text-sm"
             />
           </div>

           <div>
             <label htmlFor="gear-manualUrl" className="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
               Manual URL
             </label>
             <input
               id="gear-manualUrl"
               name="manualUrl"
               type="url"
               defaultValue={item?.manualUrl ?? ''}
               placeholder="https://..."
               className="w-full rounded-lg border border-gray-300 dark:border-gray-600 bg-white dark:bg-gray-800 px-3 py-2 text-sm"
             />
           </div>
         </div>
       </div>
       ```

    7. In the form submission handler (onSave or handleSubmit), ensure the 3 new fields are included in the data sent to the API. Find where form data is collected (either via FormData or state) and add:
       ```typescript
       modelNumber: formData.get('modelNumber') as string || null,
       connectivity: formData.get('connectivity') as string || null,
       manualUrl: formData.get('manualUrl') as string || null,
       ```
       (Adjust based on whether the form uses FormData or state variables — match the existing pattern.)

    8. Go back to GearClient.tsx and remove the `categories={CATEGORIES}` prop from where GearForm is rendered (since GearForm no longer accepts it). This makes GearForm self-contained for category data.
  </action>
  <verify>
    <automated>cd "/Users/willis/Camping Manager/.claude/worktrees/sweet-zhukovsky" && grep -n "import.*CATEGORIES.*gear-categories" components/GearForm.tsx && echo "---" && grep -n "modelNumber\|connectivity\|manualUrl" components/GearForm.tsx | head -10 && echo "---" && grep -c "categories={" components/GearClient.tsx && echo "---" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `components/GearForm.tsx` contains `import { CATEGORIES } from '@/lib/gear-categories'`
    - `components/GearForm.tsx` does NOT contain `categories: readonly CategoryOption[]` in its props interface
    - `components/GearForm.tsx` contains `id="gear-modelNumber"`
    - `components/GearForm.tsx` contains `id="gear-connectivity"`
    - `components/GearForm.tsx` contains `id="gear-manualUrl"`
    - `components/GearForm.tsx` contains `name="modelNumber"` in an input element
    - `components/GearForm.tsx` GearItem interface contains `modelNumber: string | null`
    - `components/GearClient.tsx` does NOT contain `categories={CATEGORIES}` or `categories={` passing to GearForm
    - `npx tsc --noEmit` exits 0 (no type errors from prop removal)
  </acceptance_criteria>
  <done>GearForm imports categories from shared module, has 3 new tech detail fields (modelNumber, connectivity, manualUrl), and no longer receives categories as a prop.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `npm run build` passes
- `grep -rn "const CATEGORIES\b" components/ lib/ --include="*.ts" --include="*.tsx" | grep -v gear-categories | grep -v node_modules` returns ZERO results
- GearClient renders CATEGORY_GROUPS.map for grouped display
- GearForm has modelNumber, connectivity, manualUrl fields
</verification>

<success_criteria>
- Gear page shows 15 categories in 4 visual groups with group headers
- Filter chip interaction works (click to filter, click again to clear)
- GearForm has 3 new optional tech detail fields
- GearForm imports categories directly from shared module
- No local CATEGORIES constant remains in GearClient or GearForm
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/23-gear-category-expansion/23-03-SUMMARY.md`
</output>
