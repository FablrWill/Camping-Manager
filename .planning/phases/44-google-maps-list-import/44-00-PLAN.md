---
phase: 44-google-maps-list-import
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/gmaps-import.test.ts
autonomous: true
requirements:
  - GMAPS-01
  - GMAPS-02
  - GMAPS-05

must_haves:
  truths:
    - "Test file exists with stubs covering coordinate extraction, name extraction, and zero-result error"
    - "Tests reference the GmapsPlace interface and fetchGmapsList function signature"
    - "Tests use mocked fetch with realistic HTML fixture data"
  artifacts:
    - path: "tests/gmaps-import.test.ts"
      provides: "Unit test stubs for lib/gmaps-import.ts parsing logic"
      min_lines: 60
  key_links:
    - from: "tests/gmaps-import.test.ts"
      to: "lib/gmaps-import.ts"
      via: "import { fetchGmapsList, GmapsPlace }"
      pattern: "import.*fetchGmapsList.*gmaps-import"
---

<objective>
Create Wave 0 test stubs for the Google Maps list import parsing logic. These tests define the contract for `fetchGmapsList` before implementation begins.

Purpose: Establish RED tests that specify expected behavior — coordinate extraction from `[null,null,lat,lng]` patterns, name extraction, and zero-result error handling. Implementation in Plan 01 must make these green.
Output: `tests/gmaps-import.test.ts` with failing test stubs that import from `lib/gmaps-import.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-google-maps-list-import/44-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create gmaps-import test file with fixture data and failing test stubs</name>
  <files>tests/gmaps-import.test.ts</files>
  <read_first>
    - tests/meal-plan-route.test.ts (existing test pattern — vitest imports, vi.stubGlobal usage)
    - tests/overpass.test.ts (existing test pattern for external API mocking with fetch)
    - .planning/phases/44-google-maps-list-import/44-RESEARCH.md (coordinate regex pattern, GmapsPlace interface)
  </read_first>
  <behavior>
    - Test 1 (GMAPS-01): fetchGmapsList with mocked HTML containing `[null,null,35.5951,-82.5515]` pattern returns array with at least one GmapsPlace where lat === 35.5951 and lng === -82.5515
    - Test 2 (GMAPS-02): Each extracted GmapsPlace has typeof lat === 'number' and typeof lng === 'number' (not strings)
    - Test 3 (GMAPS-05): fetchGmapsList with HTML containing zero coordinate matches throws Error with message containing 'No places found'
    - Test 4 (GMAPS-05): fetchGmapsList with a URL that redirects to accounts.google.com throws Error with message containing 'sign-in'
  </behavior>
  <action>
Create `tests/gmaps-import.test.ts` with these contents:

1. Import `describe, it, expect, vi, beforeEach, afterEach` from 'vitest'.
2. Use `require()` inside test bodies for the `fetchGmapsList` import (same pattern as Phase 33/34 — files compile before source module exists, tests fail at runtime with MODULE_NOT_FOUND until Plan 01 creates the source).

3. Define a `FIXTURE_HTML` constant — a minimal string that simulates Google Maps list HTML:
```typescript
const FIXTURE_HTML = `
<!DOCTYPE html><html><head><title>Test List</title></head><body>
<script>window.APP_INITIALIZATION_STATE=[["en",null,null,null,null,null,null,null,null,null,
"Rough Ridge Parking",null,null,null,null,null,[null,null,35.5951,-82.5515],
"Linville Gorge",null,null,null,null,null,[null,null,35.9443,-81.9231]
]];</script>
</body></html>`;
```

4. Define a `EMPTY_HTML` constant — HTML with no coordinate patterns:
```typescript
const EMPTY_HTML = `<!DOCTYPE html><html><body><p>Nothing here</p></body></html>`;
```

5. In `beforeEach`, stub global fetch with `vi.stubGlobal('fetch', vi.fn())`.
6. In `afterEach`, call `vi.restoreAllMocks()`.

7. Tests:
   - `'extracts places from [null,null,lat,lng] pattern in HTML'`: Mock fetch to return `{ ok: true, url: 'https://www.google.com/maps/...', text: () => Promise.resolve(FIXTURE_HTML) }`. Call `fetchGmapsList('https://maps.app.goo.gl/abc123')`. Assert result is array with length >= 1. Assert first item has `lat` equal to `35.5951` and `lng` equal to `-82.5515`.

   - `'extracted coordinates are numbers not strings'`: Same mock. Assert `typeof result[0].lat === 'number'` and `typeof result[0].lng === 'number'`.

   - `'throws descriptive error when no places found'`: Mock fetch to return EMPTY_HTML. Assert `fetchGmapsList(url)` rejects with error message matching `/No places found/`.

   - `'throws error when redirected to Google sign-in'`: Mock fetch to return `{ ok: true, url: 'https://accounts.google.com/signin', text: () => Promise.resolve('') }`. Assert `fetchGmapsList(url)` rejects with error message matching `/sign-in/i`.

Each test uses `require('../../lib/gmaps-import')` inside the test body (relative path from tests/ to lib/). This means tests will fail with MODULE_NOT_FOUND until Plan 01 creates the source file — this is the expected RED state.
  </action>
  <verify>
    <automated>cd "/Users/willis/Camping Manager/.claude/worktrees/loving-fermat" && npx vitest run tests/gmaps-import.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/gmaps-import.test.ts`
    - File contains `import { describe, it, expect, vi` (vitest imports)
    - File contains `FIXTURE_HTML` constant with `[null,null,35.5951,-82.5515]` pattern
    - File contains `EMPTY_HTML` constant
    - File contains at least 4 `it(` test definitions
    - File contains `vi.stubGlobal('fetch'` in beforeEach
    - File contains `vi.restoreAllMocks()` in afterEach
    - File contains `require` calls referencing `gmaps-import` (dynamic import pattern)
    - Running vitest shows all 4 tests failing (RED state — MODULE_NOT_FOUND or assertion errors)
  </acceptance_criteria>
  <done>Test file exists with 4 failing test stubs that define the contract for fetchGmapsList. All tests are RED (either MODULE_NOT_FOUND or assertion failures). Wave 1 implementation must make them green.</done>
</task>

</tasks>

<verification>
- `tests/gmaps-import.test.ts` exists with 4+ test cases
- Tests reference the correct import path (`lib/gmaps-import`)
- Tests fail (RED) when run — expected since source module does not exist yet
- Fixture HTML contains the `[null,null,lat,lng]` pattern from research
</verification>

<success_criteria>
- Test file committed to repo
- All 4 tests defined and failing (RED state)
- Test fixture data matches the coordinate regex pattern from 44-RESEARCH.md
</success_criteria>

<output>
After completion, create `.planning/phases/44-google-maps-list-import/44-00-SUMMARY.md`
</output>
