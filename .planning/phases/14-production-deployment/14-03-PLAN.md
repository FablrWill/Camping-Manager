---
phase: 14-production-deployment
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - docs/MAC-MINI-SETUP.md
autonomous: false
requirements: [DEPLOY-05]

must_haves:
  truths:
    - "Mac mini setup guide exists with step-by-step instructions for first-time deployment"
    - "Guide covers directory creation, git clone, .env setup, PM2 install, pm2 startup, crontab, and first deploy"
    - "Build verification passes (npm run build succeeds with standalone output)"
  artifacts:
    - path: "docs/MAC-MINI-SETUP.md"
      provides: "One-time Mac mini setup instructions"
      contains: "pm2 startup"
  key_links:
    - from: "docs/MAC-MINI-SETUP.md"
      to: "deploy.sh"
      via: "References deploy.sh as the deploy command"
      pattern: "deploy\\.sh"
    - from: "docs/MAC-MINI-SETUP.md"
      to: "scripts/outland-backup.sh"
      via: "References backup script for crontab setup"
      pattern: "outland-backup\\.sh"
---

<objective>
Create the Mac mini setup guide and verify the production build works end-to-end.

Purpose: Will needs a single reference document to follow when setting up the Mac mini for the first time. This plan also verifies that `npm run build` produces a working standalone output, confirming all the code changes from Plans 01-02 integrate correctly.

Output: Setup guide document and verified production build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-production-deployment/14-CONTEXT.md
@.planning/phases/14-production-deployment/14-RESEARCH.md
@.planning/phases/14-production-deployment/14-01-SUMMARY.md
@.planning/phases/14-production-deployment/14-02-SUMMARY.md

<interfaces>
<!-- From Plans 01 and 02 -->

Files created/modified:
- next.config.ts (output: 'standalone')
- lib/paths.ts (getPhotosDir, resolvePhotoPath)
- app/api/health/route.ts (GET → JSON status)
- ecosystem.config.js (PM2 fork mode config)
- deploy.sh (one-command deploy)
- scripts/outland-backup.sh (daily backup + iMessage)
- scripts/outland-watchdog.sh (crash detection + iMessage)
- .env.example (PHOTOS_DIR, DATABASE_URL, DATA_DIR, NOTIFY_NUMBER)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mac mini setup guide + build verification</name>
  <files>docs/MAC-MINI-SETUP.md</files>
  <read_first>
    - .env.example (all env vars that need to be in production .env)
    - ecosystem.config.js (PM2 config to reference)
    - deploy.sh (deploy workflow to reference)
    - scripts/outland-backup.sh (backup crontab entry)
    - scripts/outland-watchdog.sh (watchdog crontab entry)
    - .planning/phases/14-production-deployment/14-RESEARCH.md (lines 405-428 for one-time setup steps and runtime state)
  </read_first>
  <action>
    **1. Verify the build works:**

    Run `npm run build` and confirm it produces `.next/standalone/server.js`. This validates that `output: 'standalone'` in next.config.ts works with the existing codebase.

    **2. Create `docs/MAC-MINI-SETUP.md`:**

    Write a step-by-step setup guide for Will to follow on his Mac mini. The guide must cover:

    **Prerequisites section:**
    - Node.js 20 LTS installed
    - git installed
    - Apple ID signed into Messages app

    **Step 1 — Create data directory (DEPLOY-02):**
    ```bash
    sudo mkdir -p /data/outland
    sudo chown -R $(whoami) /data/outland
    mkdir -p /data/outland/{photos,backups,logs}
    ```

    **Step 2 — Clone the repo:**
    ```bash
    mkdir -p ~/outland
    git clone <repo-url> ~/outland
    cd ~/outland
    ```

    **Step 3 — Create production .env:**
    - Copy from .env.example: `cp .env.example .env`
    - Edit to set production values:
      - `DATABASE_URL="file:/data/outland/db.sqlite"`
      - `PHOTOS_DIR=/data/outland/photos`
      - `DATA_DIR=/data/outland`
      - `NOTIFY_NUMBER=+1XXXXXXXXXX` (Will's actual phone number)
      - Uncomment and fill in: ANTHROPIC_API_KEY, OPENAI_API_KEY, VOYAGE_API_KEY, GMAIL_USER, GMAIL_APP_PASSWORD

    **Step 4 — Install PM2 globally (DEPLOY-04):**
    ```bash
    npm install -g pm2
    ```

    **Step 5 — First deploy:**
    ```bash
    chmod +x deploy.sh scripts/*.sh
    ./deploy.sh
    ```

    **Step 6 — Set up PM2 boot persistence (DEPLOY-05):**
    ```bash
    pm2 startup
    # Run the sudo command it outputs
    pm2 save
    ```
    Note: This uses launchd on macOS (not systemd). The generated plist lives at `~/Library/LaunchAgents/`.

    **Step 7 — Set up crontab for backups and watchdog (DEPLOY-07, D-03, D-07):**
    ```bash
    crontab -e
    ```
    Add these two lines:
    ```
    0 2 * * * ~/outland/scripts/outland-backup.sh >> /data/outland/logs/backup.log 2>&1
    */5 * * * * ~/outland/scripts/outland-watchdog.sh 2>/dev/null
    ```

    **Step 8 — Test iMessage notifications:**
    ```bash
    osascript -e 'tell application "Messages" to send "Outland setup test" to buddy "+1XXXXXXXXXX"'
    ```

    **Step 9 — Verify everything works:**
    - `curl http://localhost:3000/api/health` returns JSON with `status: 'ok'`
    - `pm2 list` shows `outland` with status `online`
    - `pm2 show outland` shows fork mode, correct log paths

    **Subsequent deploys section:**
    ```bash
    ssh mac-mini 'cd ~/outland && ./deploy.sh'
    ```

    **Optional: Migrate existing photos section:**
    ```bash
    # From MacBook, one-time:
    rsync -avz public/photos/ mac-mini:/data/outland/photos/
    ```

    **Troubleshooting section** with the known pitfalls from research:
    - App not accessible from Tailscale: check HOSTNAME binding
    - Photos showing as broken: check photo symlink
    - PM2 not starting after reboot: run `pm2 save` again
    - Backup iMessage not arriving: check Messages app is signed in
  </action>
  <verify>
    <automated>cd "/Users/willis/Camping Manager/.claude/worktrees/gifted-banzai" && npm run build 2>&1 | tail -5 && test -f .next/standalone/server.js && test -f docs/MAC-MINI-SETUP.md && grep -q "pm2 startup" docs/MAC-MINI-SETUP.md && grep -q "deploy.sh" docs/MAC-MINI-SETUP.md && grep -q "crontab" docs/MAC-MINI-SETUP.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds and produces `.next/standalone/server.js`
    - docs/MAC-MINI-SETUP.md exists
    - docs/MAC-MINI-SETUP.md contains `sudo mkdir -p /data/outland`
    - docs/MAC-MINI-SETUP.md contains `pm2 startup` (DEPLOY-05)
    - docs/MAC-MINI-SETUP.md contains `pm2 save`
    - docs/MAC-MINI-SETUP.md contains `crontab` with backup schedule
    - docs/MAC-MINI-SETUP.md contains `deploy.sh` reference
    - docs/MAC-MINI-SETUP.md contains `outland-backup.sh` reference
    - docs/MAC-MINI-SETUP.md contains `outland-watchdog.sh` reference
    - docs/MAC-MINI-SETUP.md contains env var setup for DATABASE_URL, PHOTOS_DIR, NOTIFY_NUMBER
    - docs/MAC-MINI-SETUP.md contains troubleshooting section
  </acceptance_criteria>
  <done>Build produces standalone server.js, Mac mini setup guide committed with complete step-by-step instructions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production build output</name>
  <what-built>
    Complete production deployment configuration:
    1. Next.js standalone build (server.js) — verified by successful `npm run build`
    2. Photo path migration — all 3 routes use getPhotosDir()/resolvePhotoPath() from lib/paths.ts
    3. Health endpoint at /api/health — returns JSON with status, uptime, lastBackup
    4. PM2 ecosystem.config.js — fork mode, correct paths, log rotation
    5. deploy.sh — one-command deploy with build-then-swap safety
    6. Backup script — daily sqlite3 .backup with iMessage failure alert
    7. Watchdog script — PM2 crash detection with iMessage alert
    8. Mac mini setup guide — step-by-step first-time setup instructions
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000/api/health — should return JSON with `status: "ok"` and `uptime` fields
    2. Review deploy.sh: `cat deploy.sh` — confirm it matches the expected workflow (pull, install, version bump, migrate, build, copy assets, symlink photos, restart PM2)
    3. Review ecosystem.config.js: `cat ecosystem.config.js` — confirm fork mode, port 3000, log paths
    4. Review docs/MAC-MINI-SETUP.md — confirm the setup steps make sense for your Mac mini
    5. Optionally: `ls .next/standalone/server.js` — confirm standalone build output exists
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 14, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` succeeds — standalone build works with all code changes
2. `.next/standalone/server.js` exists — standalone output produced
3. `docs/MAC-MINI-SETUP.md` exists — setup guide committed
4. Health endpoint returns JSON when dev server is running
</verification>

<success_criteria>
- Production build succeeds with standalone output
- Mac mini setup guide covers all required steps (directory, clone, .env, PM2, cron)
- Will has verified the build output and deployment configuration
</success_criteria>

<output>
After completion, create `.planning/phases/14-production-deployment/14-03-SUMMARY.md`
</output>
