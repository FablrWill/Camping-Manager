---
phase: 37-home-assistant-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - lib/ha.ts
  - app/api/settings/route.ts
autonomous: true
requirements: [HA-01]

must_haves:
  truths:
    - "Settings model has haUrl, haToken, haEntityIds, haEntityCache, haLastFetched fields"
    - "GET /api/settings never returns haToken value"
    - "PUT /api/settings accepts and stores HA fields"
    - "HA helper types and utility functions exist for downstream routes"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Settings model with HA fields"
      contains: "haUrl"
    - path: "lib/ha.ts"
      provides: "HA types, constants, utility functions"
      exports: ["HAEntityState", "CachedEntityState", "safeParseEntityIds", "SETTINGS_ID", "HA_TIMEOUT_MS"]
    - path: "app/api/settings/route.ts"
      provides: "Settings GET (token omission) and PUT (HA fields)"
      contains: "haTokenSet"
  key_links:
    - from: "app/api/settings/route.ts"
      to: "prisma.settings"
      via: "Prisma findUnique/upsert"
      pattern: "prisma\\.settings\\."
    - from: "lib/ha.ts"
      to: "prisma/schema.prisma"
      via: "Type alignment with Prisma model"
      pattern: "CachedEntityState"
---

<objective>
Create the data foundation for Home Assistant integration: extend the Settings singleton with HA fields via Prisma migration, create shared HA types and utility functions, and update the Settings API to handle HA fields with write-only token security.

Purpose: All downstream HA plans (API routes, UI components) depend on these types, the schema, and the Settings API updates existing before they can be built.
Output: Prisma migration applied, lib/ha.ts with shared types/utils, Settings GET/PUT handling HA fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-home-assistant-integration/37-CONTEXT.md
@.planning/phases/37-home-assistant-integration/37-RESEARCH.md

@prisma/schema.prisma
@app/api/settings/route.ts
@lib/db.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prisma migration + HA types and utility lib</name>
  <files>prisma/schema.prisma, lib/ha.ts</files>
  <read_first>
    - prisma/schema.prisma (current Settings model at line ~337)
    - lib/db.ts (Prisma client import pattern)
    - app/api/settings/route.ts (SETTINGS_ID constant pattern)
  </read_first>
  <behavior>
    - safeParseEntityIds(null) returns []
    - safeParseEntityIds('["sensor.battery"]') returns ["sensor.battery"]
    - safeParseEntityIds('invalid json') returns []
    - safeParseEntityIds(undefined as unknown as string | null) returns []
    - normalizeHaUrl('http://ha.local:8123/') returns 'http://ha.local:8123'
    - normalizeHaUrl('http://ha.local:8123') returns 'http://ha.local:8123'
    - stripHaToken strips haToken from settings object and adds haTokenSet boolean
  </behavior>
  <action>
1. Add five fields to the Settings model in prisma/schema.prisma (per D-04):
   ```prisma
   haUrl           String?
   haToken         String?
   haEntityIds     String?
   haEntityCache   String?
   haLastFetched   DateTime?
   ```
   Place them after `updatedAt` in the Settings model block.

2. Run `npx prisma migrate dev --name add-ha-settings-fields` to create and apply migration.

3. Create `lib/ha.ts` with:
   - `SETTINGS_ID = 'user_settings'` constant (reused from settings route)
   - `HA_TIMEOUT_MS = 8000` constant (8s timeout per Claude's discretion)
   - `HAEntityState` interface matching HA REST API shape:
     ```typescript
     export interface HAEntityState {
       entity_id: string;
       state: string;
       last_changed: string;
       last_updated: string;
       attributes: {
         friendly_name?: string;
         unit_of_measurement?: string;
         [key: string]: unknown;
       };
     }
     ```
   - `CachedEntityState` interface for compact cache:
     ```typescript
     export interface CachedEntityState {
       entityId: string;
       friendlyName: string;
       state: string;
       unit: string;
     }
     ```
   - `safeParseEntityIds(raw: string | null): string[]` — wraps JSON.parse in try/catch, returns [] on null/undefined/malformed (prevents Pitfall 3)
   - `normalizeHaUrl(url: string): string` — strips trailing slash (prevents Pitfall 5): `return url.replace(/\/+$/, '')`
   - `stripHaToken(settings: Record<string, unknown>): Record<string, unknown> & { haTokenSet: boolean }` — destructures out haToken, adds `haTokenSet: Boolean(haToken)` (per D-05, Pattern 2 from RESEARCH.md)
   - `isUnavailableState(state: string): boolean` — returns true for 'unavailable' or 'unknown' (Pitfall 6)

4. Create test file `tests/ha-lib.test.ts` with tests for safeParseEntityIds, normalizeHaUrl, stripHaToken, isUnavailableState per the behavior block above. Follow TDD: write tests first, run (RED), then implement, run (GREEN).
  </action>
  <verify>
    <automated>npm test -- tests/ha-lib.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains `haUrl           String?` inside model Settings
    - prisma/schema.prisma contains `haToken         String?` inside model Settings
    - prisma/schema.prisma contains `haEntityIds     String?` inside model Settings
    - prisma/schema.prisma contains `haEntityCache   String?` inside model Settings
    - prisma/schema.prisma contains `haLastFetched   DateTime?` inside model Settings
    - prisma/migrations/ contains a directory named *add-ha-settings-fields
    - lib/ha.ts contains `export interface HAEntityState`
    - lib/ha.ts contains `export interface CachedEntityState`
    - lib/ha.ts contains `export function safeParseEntityIds`
    - lib/ha.ts contains `export function normalizeHaUrl`
    - lib/ha.ts contains `export function stripHaToken`
    - lib/ha.ts contains `export function isUnavailableState`
    - lib/ha.ts contains `export const SETTINGS_ID`
    - lib/ha.ts contains `export const HA_TIMEOUT_MS`
    - tests/ha-lib.test.ts exits 0 with all tests passing
  </acceptance_criteria>
  <done>Settings model has 5 HA fields, migration applied, lib/ha.ts exports all types and utilities, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update Settings API route for HA fields</name>
  <files>app/api/settings/route.ts</files>
  <read_first>
    - app/api/settings/route.ts (current GET/PUT implementation)
    - lib/ha.ts (stripHaToken, normalizeHaUrl, SETTINGS_ID — created in Task 1)
  </read_first>
  <action>
1. Update `GET /api/settings` to strip haToken from response (per D-05):
   - After fetching settings from Prisma, destructure: `const { haToken, ...safeSettings } = settings`
   - Return `{ ...safeSettings, haTokenSet: Boolean(haToken) }` instead of raw settings
   - Import `stripHaToken` from `@/lib/ha` and use it, OR inline the destructure (simpler since it's 2 lines)
   - The default empty response (when no settings exist) should also include `haTokenSet: false`

2. Update `PUT /api/settings` to accept HA fields alongside existing fields:
   - Accept optional body fields: `haUrl?: string`, `haToken?: string`, `haEntityIds?: string`
   - Change validation: name/email should NOT be required when only saving HA fields. Make the route accept partial updates:
     - If body has `name` and `email`, save emergency contact fields (existing behavior)
     - If body has `haUrl` or `haToken` or `haEntityIds`, save HA fields
     - At least one group of fields must be present (return 400 if body is empty)
   - When saving haUrl: normalize with `url.replace(/\/+$/, '')` before storing (Pitfall 5)
   - When saving haToken: only update if non-empty string (allows saving URL without clearing token)
   - Build the `update` object dynamically based on which fields are present in the request body
   - Response from PUT must also strip haToken (same as GET): return `{ ...result, haTokenSet: Boolean(result.haToken) }` then delete result.haToken from the response

3. Import `SETTINGS_ID` from `@/lib/ha` and remove the local `const SETTINGS_ID = 'user_settings'` to use the shared constant.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/api/settings/route.ts contains `import` from `@/lib/ha`
    - app/api/settings/route.ts contains `haTokenSet` in the GET response
    - app/api/settings/route.ts does NOT contain a line that returns `haToken` directly in JSON response (grep for `haToken` should only appear in destructure/omission patterns)
    - app/api/settings/route.ts contains `haUrl` in the PUT handler
    - app/api/settings/route.ts contains `replace(/\/+$/, '')` or equivalent trailing slash strip
    - npm run build exits 0
  </acceptance_criteria>
  <done>Settings GET returns haTokenSet instead of haToken, PUT accepts and stores HA config fields with URL normalization, build passes</done>
</task>

</tasks>

<verification>
- `npx prisma migrate status` shows all migrations applied
- `npm test -- tests/ha-lib.test.ts` passes
- `npm run build` passes
- GET /api/settings response does not contain haToken field
</verification>

<success_criteria>
- Settings model extended with 5 HA fields and migration applied
- lib/ha.ts exports shared types (HAEntityState, CachedEntityState) and utilities (safeParseEntityIds, normalizeHaUrl, stripHaToken, isUnavailableState)
- Settings GET omits haToken, returns haTokenSet boolean
- Settings PUT accepts haUrl, haToken, haEntityIds alongside existing fields
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/37-home-assistant-integration/37-01-SUMMARY.md`
</output>
