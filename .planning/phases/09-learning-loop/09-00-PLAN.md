---
phase: 09-learning-loop
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/usage-tracking.test.ts
  - tests/trip-summary.test.ts
  - tests/voice-debrief.test.ts
autonomous: true
requirements:
  - LEARN-01
  - LEARN-02
  - LEARN-03

must_haves:
  truths:
    - "All Phase 9 test stub files exist and are parseable by Vitest"
    - "Running npx vitest run exits without config errors"
  artifacts:
    - path: "tests/usage-tracking.test.ts"
      provides: "Test stubs for LEARN-01 usage status PATCH"
      contains: "describe"
    - path: "tests/trip-summary.test.ts"
      provides: "Test stubs for LEARN-02 summary generation"
      contains: "describe"
    - path: "tests/voice-debrief.test.ts"
      provides: "Test stubs for LEARN-03 TripFeedback persistence"
      contains: "describe"
  key_links: []
---

<objective>
Create Vitest test stub files for all Phase 9 modules so that every subsequent plan has an automated verify target from the start.

Purpose: Nyquist compliance — every task in Plans 01-03 must have an automated verify command that references a real test file.
Output: Three test stub files with describe blocks and placeholder tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-learning-loop/09-CONTEXT.md
@.planning/phases/09-learning-loop/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files for Phase 9 modules</name>
  <files>tests/usage-tracking.test.ts, tests/trip-summary.test.ts, tests/voice-debrief.test.ts</files>
  <read_first>
    - vitest.config.ts
    - tests/ (list directory to see existing test file naming patterns)
  </read_first>
  <action>
Create three test stub files following Vitest conventions.

**tests/usage-tracking.test.ts:**
```typescript
import { describe, it, expect } from 'vitest'

describe('Usage Tracking (LEARN-01)', () => {
  describe('PATCH /api/trips/[id]/usage', () => {
    it.todo('returns 400 when gearId is missing')
    it.todo('updates PackingItem.usageStatus to "used"')
    it.todo('updates PackingItem.usageStatus to "didn\'t need"')
    it.todo('updates PackingItem.usageStatus to "forgot but needed"')
    it.todo('returns 404 for non-existent tripId_gearId combo')
  })

  describe('GET /api/packing-list includes usageState', () => {
    it.todo('response includes usageState map alongside packedState')
  })
})
```

**tests/trip-summary.test.ts:**
```typescript
import { describe, it, expect } from 'vitest'

describe('Trip Summary (LEARN-02)', () => {
  describe('TripSummaryResultSchema', () => {
    it.todo('validates valid summary result')
    it.todo('rejects missing summary field')
    it.todo('defaults whatToDrop and whatWasMissing to empty arrays')
    it.todo('validates locationRating is 1-5 or null')
  })

  describe('POST /api/trips/[id]/feedback', () => {
    it.todo('returns existing summary if one exists (no duplicate generation)')
    it.todo('returns 400 if not all packing items have usageStatus')
  })

  describe('auto-generate trigger', () => {
    it.todo('detects completion when all items have non-null usageStatus')
    it.todo('does not trigger when some items still null')
    it.todo('allComplete is true only when every item has non-null usageStatus')
  })
})
```

**tests/voice-debrief.test.ts:**
```typescript
import { describe, it, expect } from 'vitest'

describe('Voice Debrief Persistence (LEARN-03)', () => {
  describe('POST /api/voice/apply with TripFeedback', () => {
    it.todo('creates TripFeedback record with voiceTranscript')
    it.todo('creates TripFeedback record without voiceTranscript when not provided')
    it.todo('stores insights as JSON string')
    it.todo('sets status to "applied"')
    it.todo('existing apply route write-back works unchanged after TripFeedback addition')
  })

  describe('apply route regression', () => {
    it.todo('gear note write-back still executes when TripFeedback is added')
    it.todo('location rating update still executes when TripFeedback is added')
    it.todo('response shape { applied: results } is unchanged')
  })

  describe('ApplyInsightRequest type', () => {
    it.todo('accepts optional voiceTranscript field')
  })
})
```
  </action>
  <verify>
    <automated>npx vitest run tests/usage-tracking.test.ts tests/trip-summary.test.ts tests/voice-debrief.test.ts --reporter=verbose 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - tests/usage-tracking.test.ts contains "describe('Usage Tracking (LEARN-01)'"
    - tests/trip-summary.test.ts contains "describe('Trip Summary (LEARN-02)'"
    - tests/trip-summary.test.ts contains "allComplete is true only when every item"
    - tests/voice-debrief.test.ts contains "describe('Voice Debrief Persistence (LEARN-03)'"
    - tests/voice-debrief.test.ts contains "existing apply route write-back works unchanged after TripFeedback addition"
    - tests/voice-debrief.test.ts contains "gear note write-back still executes"
    - tests/voice-debrief.test.ts contains "location rating update still executes"
    - tests/voice-debrief.test.ts contains "response shape { applied: results } is unchanged"
    - npx vitest run exits with code 0 (todo tests are not failures)
  </acceptance_criteria>
  <done>Three test stub files exist with describe blocks including regression stubs for apply route, Vitest runs without errors</done>
</task>

</tasks>

<verification>
npx vitest run --reporter=verbose 2>&1 | tail -10
</verification>

<success_criteria>
- All three test files exist and import from vitest
- npx vitest run passes (todo tests do not fail)
- Test stubs cover LEARN-01, LEARN-02, LEARN-03
- Voice debrief stubs include regression coverage for existing apply route behavior
- Trip summary stubs include allComplete detection coverage
</success_criteria>

<output>
After completion, create `.planning/phases/09-learning-loop/09-00-SUMMARY.md`
</output>
