---
phase: 12-fix-build-clean-house
plan: 05
type: execute
wave: 2
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - .planning/ROADMAP.md
  - TASKS.md
autonomous: true
requirements: [BUILD-01, BUILD-02, BUILD-04, BUILD-10]

must_haves:
  truths:
    - "npm run build completes without errors"
    - "npm run lint completes with 0 errors"
    - "npx tsc --noEmit completes with 0 errors"
    - "npm test completes with 0 failures and 0 it.todo items"
    - "ROADMAP.md has correct v1.2 content with all completed phases checked"
    - "TASKS.md reflects Phase 12 completion and points Next to Phase 13"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Consistent roadmap with v1.2 header and completed phase checkboxes"
      contains: "v1.2 Ship It"
    - path: "TASKS.md"
      provides: "Updated task tracker reflecting Phase 12 completion"
      contains: "Phase 12"
  key_links: []
---

<objective>
Verify BUILD-01/BUILD-02 are already complete, fix ROADMAP.md if needed (BUILD-10), run the full D-10 verification pipeline to gate the phase, and update TASKS.md to reflect completion.

Purpose: Final verification gate ensuring all phase work is complete, the four commands from D-10 all pass, and project tracking documents are current per CLAUDE.md mandate.
Output: Verified build, verified ROADMAP, clean pass on all four verification commands, updated TASKS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-fix-build-clean-house/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify BUILD-01/BUILD-02 and fix ROADMAP.md (BUILD-10)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - next.config.ts (verify serverExternalPackages contains better-sqlite3 and sqlite-vec — BUILD-01/BUILD-02)
    - .planning/ROADMAP.md (check for v1.2 header, completed phase checkboxes, plan counts)
  </read_first>
  <action>
    **BUILD-01/BUILD-02 verification (no code changes expected):**
    1. Read `next.config.ts` and confirm `serverExternalPackages` array contains `'better-sqlite3'` and `'sqlite-vec'`
    2. Run `npm run build` and confirm it completes without errors
    3. If build fails (unlikely — research confirmed it passes), diagnose and fix

    **BUILD-10 — ROADMAP.md consistency:**
    Read `.planning/ROADMAP.md` and verify:
    1. The milestone header shows "v1.2 Ship It" (not "v1.1 Close the Loop")
    2. All Phase 1-11 entries are marked `[x]` (completed)
    3. Phase 12-15 entries are marked `[ ]` (not started)
    4. Plan counts match reality for completed phases

    If the ROADMAP.md already has correct v1.2 content (which appears to be the case based on the current working tree), confirm and move on. If any inconsistencies exist:
    - Fix header to reference v1.2 Ship It milestone
    - Ensure all completed phases (1-11) have `[x]` checkboxes
    - Ensure Phase 12 entry shows the correct plan count once known (update after plans 01-04 are created)

    Update Phase 12 entry with actual plan count:
    ```
    - [ ] **Phase 12: Fix Build & Clean House** - Fix broken build, resolve tech debt, run Gemini review in parallel
    ```
    Add plan list under Phase 12:
    ```
    Plans:
    - [ ] 12-01-PLAN.md — Component fixes + lint errors
    - [ ] 12-02-PLAN.md — SW runtime cache + tripCoords pipe
    - [ ] 12-03-PLAN.md — Test stub implementations
    - [ ] 12-04-PLAN.md — Gemini cross-AI review
    - [ ] 12-05-PLAN.md — ROADMAP fix + final verification gate
    ```

    **BUILD-04 final verification:**
    Run `grep -r "variant=\"outline\"" components/ app/` and confirm 0 results. BUILD-04 was verified as a no-op in plan 01 but this is the final confirmation.
  </action>
  <verify>
    <automated>cd "/Users/willis/Camping Manager/.claude/worktrees/elegant-hodgkin" && grep "serverExternalPackages" next.config.ts && grep -c "variant=\"outline\"" components/*.tsx app/**/*.tsx 2>/dev/null; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains `serverExternalPackages` with `better-sqlite3` and `sqlite-vec`
    - `grep -r "variant=\"outline\"" components/ app/` returns 0 matches
    - `.planning/ROADMAP.md` contains "v1.2 Ship It"
    - All Phase 1-11 entries in ROADMAP.md have `[x]` checkboxes
  </acceptance_criteria>
  <done>BUILD-01/02 verified as already complete. BUILD-04 verified as no-op. ROADMAP.md is consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Run full D-10 verification pipeline (all four commands must pass)</name>
  <files></files>
  <read_first>
    - .planning/phases/12-fix-build-clean-house/12-CONTEXT.md (D-10 definition: npm run build + npm run lint + tsc --noEmit + npm test)
  </read_first>
  <action>
    Run all four verification commands sequentially. All must pass for the phase to be complete (per D-10):

    ```bash
    # Command 1: Production build
    npm run build

    # Command 2: Lint (must be 0 errors — warnings acceptable)
    npm run lint

    # Command 3: Type checking
    npx tsc --noEmit

    # Command 4: Test suite
    npm test
    ```

    If any command fails:
    - **Build failure**: Check for newly introduced import errors from plan 01-03 changes. Fix the specific file.
    - **Lint failure**: If new lint errors were introduced by other plans, fix them. If pre-existing errors remain from plan 01, flag as incomplete.
    - **Type error**: Check for mismatched prop types from plan 02 (tripCoords threading). Fix the type.
    - **Test failure**: Check for mock setup issues from plan 03. Fix the test.

    Report the output of all four commands in the summary.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/bin:$PATH" && cd "/Users/willis/Camping Manager/.claude/worktrees/elegant-hodgkin" && npm run build 2>&1 | tail -3 && npm run lint 2>&1 | tail -3 && npx tsc --noEmit 2>&1 | tail -3 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - `npm run lint` output shows 0 errors (line containing "0 errors" or no error lines)
    - `npx tsc --noEmit` exits with code 0 and produces no error output
    - `npm test` exits with code 0, all tests pass, output contains no "FAIL" or "it.todo"
  </acceptance_criteria>
  <done>All four D-10 verification commands pass. Phase 12 tech debt work is complete and verified.</done>
</task>

<task type="auto">
  <name>Task 3: Update TASKS.md to reflect Phase 12 completion (CLAUDE.md compliance)</name>
  <files>TASKS.md</files>
  <read_first>
    - TASKS.md (current content — stale with v1.1 status, needs v1.2 Phase 12 update)
    - CLAUDE.md (mandate: "TASKS.md is the single source of truth. Update it every session." and "When a feature is built, mark it Done immediately")
  </read_first>
  <action>
    This task runs ONLY after Task 2 passes (D-10 gate confirmed). Per CLAUDE.md, TASKS.md must be updated every session and features marked Done immediately.

    1. Read current TASKS.md
    2. Add a "Milestone v1.2 — Ship It" section if not present (or update existing)
    3. Add Phase 12 entry with status "Done":
       ```
       | 12. Fix Build & Clean House | Done | Build fix (serverExternalPackages), lint/type errors resolved, test stubs implemented, Gemini cross-review completed |
       ```
    4. Update the "Last updated" line at the top to reflect the current session
    5. Update the "Next" pointer to reference Phase 13 (whatever it is per ROADMAP.md)
    6. Do NOT change any v1.0 or v1.1 content — only add/update v1.2 section

    Keep the format consistent with existing milestone tables in the file.
  </action>
  <verify>
    <automated>cd "/Users/willis/Camping Manager/.claude/worktrees/elegant-hodgkin" && grep -c "Phase 12" TASKS.md && grep "Next:" TASKS.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - TASKS.md contains a v1.2 section with Phase 12 marked as complete/done
    - "Next" pointer references Phase 13 (not Phase 12 or earlier)
    - "Last updated" line reflects current session
    - No v1.0 or v1.1 content was modified
  </acceptance_criteria>
  <done>TASKS.md updated with Phase 12 completion status, Next pointer advanced to Phase 13, per CLAUDE.md mandate.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `npm run lint` exits with 0 errors
- `npx tsc --noEmit` exits 0
- `npm test` exits 0, 0 todos, 0 failures
- ROADMAP.md is consistent with v1.2 content
- TASKS.md reflects Phase 12 completion with Next pointing to Phase 13
</verification>

<success_criteria>
- BUILD-01/BUILD-02 confirmed as already complete (serverExternalPackages in place, build passes)
- BUILD-04 confirmed as no-op (no variant="outline" in codebase)
- BUILD-10 ROADMAP.md consistency verified/fixed
- Full D-10 gate: all four commands pass clean
- TASKS.md updated per CLAUDE.md mandate
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-build-clean-house/12-05-SUMMARY.md`
</output>
