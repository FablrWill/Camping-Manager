---
phase: 12-fix-build-clean-house
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/PackingList.tsx
  - components/MealPlan.tsx
  - components/SettingsClient.tsx
  - app/spots/spots-client.tsx
  - components/InstallBanner.tsx
  - components/LeavingNowButton.tsx
  - components/ThemeProvider.tsx
  - components/OfflineBanner.tsx
autonomous: true
requirements: [BUILD-03, BUILD-04, BUILD-05]

must_haves:
  truths:
    - "No raw <button> elements in PackingList.tsx or MealPlan.tsx (all use design system Button)"
    - "No variant='outline' exists anywhere in the codebase"
    - "No placeholder 'coming in future phases' card in SettingsClient.tsx"
    - "npm run lint exits with 0 errors (warnings are acceptable)"
  artifacts:
    - path: "components/PackingList.tsx"
      provides: "Design system Button usage for all action buttons"
      contains: "import.*Button.*from.*@/components/ui"
    - path: "components/MealPlan.tsx"
      provides: "Design system Button usage for all action buttons"
      contains: "import.*Button.*from.*@/components/ui"
    - path: "components/SettingsClient.tsx"
      provides: "Placeholder card removed"
  key_links:
    - from: "components/PackingList.tsx"
      to: "components/ui/Button.tsx"
      via: "import"
      pattern: "import.*Button.*from.*@/components/ui"
    - from: "components/MealPlan.tsx"
      to: "components/ui/Button.tsx"
      via: "import"
      pattern: "import.*Button.*from.*@/components/ui"
---

<objective>
Replace raw `<button>` elements with design system Button component in PackingList and MealPlan, remove the SettingsClient placeholder card, verify BUILD-04 is already complete, and fix all 6 pre-existing lint errors so `npm run lint` passes with 0 errors.

Purpose: Clean up design system inconsistencies and fix lint errors that block the D-10 verification gate.
Output: Components using design system Button, no placeholder cards, lint-clean codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-fix-build-clean-house/12-RESEARCH.md

<interfaces>
<!-- Design system Button component interface -->
From components/ui/Button.tsx:
```typescript
type ButtonVariant = 'primary' | 'secondary' | 'danger' | 'ghost'
type ButtonSize = 'sm' | 'md' | 'lg'

interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ButtonVariant
  size?: ButtonSize
  loading?: boolean
  icon?: React.ReactNode
}
// Default: variant='primary', size='md'
// The className prop can be used to add conditional classes on top of variant styles.
// For conditional selected/unselected styling, use `ghost` variant + className override.
```

Variant styles:
- primary: amber-600 bg, white text
- secondary: border stone-300, stone-700 text
- danger: red-600 bg, white text
- ghost: transparent bg, stone-500 text, hover stone-100 bg
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace raw buttons in PackingList and MealPlan with design system Button (BUILD-03), verify BUILD-04, remove SettingsClient placeholder (BUILD-05)</name>
  <files>components/PackingList.tsx, components/MealPlan.tsx, components/SettingsClient.tsx</files>
  <read_first>
    - components/ui/Button.tsx (design system Button interface and variant styles)
    - components/PackingList.tsx (current raw button locations: lines 198, 277, 390, 396, 407)
    - components/MealPlan.tsx (current raw button locations: lines 161, 239, 265, 331, 354, 368)
    - components/SettingsClient.tsx (placeholder card at lines 177-182)
    - components/PostTripReview.tsx (verify no variant="outline" exists — BUILD-04 is a no-op)
  </read_first>
  <action>
    **PackingList.tsx (5 raw buttons):**
    1. Add `import { Button } from '@/components/ui'` at top (or add Button to existing ui import if one exists)
    2. Replace each `<button>` with `<Button>`, mapping existing classes to variant + className:
       - Action buttons (generate, regenerate, etc): use `variant="primary"` or `variant="secondary"` depending on visual hierarchy
       - Toggle/filter buttons with conditional selected state: use `variant="ghost"` with the existing conditional className for selected state styling via the `className` prop
       - Small utility buttons: use `size="sm"`
    3. Keep any `onClick`, `disabled`, `type` props as-is — ButtonProps extends HTMLButtonAttributes
    4. Do NOT replace STATUS_OPTIONS chip buttons in PostTripReview.tsx — those are pill-style selection chips, not standard action buttons

    **MealPlan.tsx (6 raw buttons):**
    1. Same pattern as PackingList — add Button import, replace raw `<button>` with `<Button>`
    2. Map the same way: action buttons to primary/secondary, toggles to ghost+className

    **SettingsClient.tsx:**
    1. Remove lines 177-182 (the `<Card>` containing "More settings coming in future phases." text)
    2. This is the last Card in the component — just delete the block, keep the closing `</div>`

    **BUILD-04 verification:**
    Run `grep -r "variant=\"outline\"" components/ app/` — expect 0 results. BUILD-04 is already complete (PostTripReview.tsx uses variant="secondary" not "outline"). No code changes needed.
  </action>
  <verify>
    <automated>cd "/Users/willis/Camping Manager/.claude/worktrees/elegant-hodgkin" && grep -c "<button" components/PackingList.tsx && grep -c "<button" components/MealPlan.tsx && grep -c "coming in future" components/SettingsClient.tsx && grep -rc "variant=\"outline\"" components/ app/ || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep "<button" components/PackingList.tsx` returns 0 matches
    - `grep "<button" components/MealPlan.tsx` returns 0 matches
    - `grep "coming in future" components/SettingsClient.tsx` returns 0 matches
    - `grep -r "variant=\"outline\"" components/ app/` returns 0 matches
    - Both files contain `import.*Button.*from.*@/components/ui`
  </acceptance_criteria>
  <done>All raw buttons in PackingList and MealPlan replaced with design system Button. SettingsClient placeholder removed. BUILD-04 verified as no-op.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all 6 pre-existing lint errors (setState-in-effect and impure-function-in-render)</name>
  <files>app/spots/spots-client.tsx, components/InstallBanner.tsx, components/LeavingNowButton.tsx, components/ThemeProvider.tsx, components/OfflineBanner.tsx</files>
  <read_first>
    - app/spots/spots-client.tsx (errors at lines 74 and 129 — setState-in-effect)
    - components/InstallBanner.tsx (error at line 21 — setState-in-effect)
    - components/LeavingNowButton.tsx (error at line 43 — memoization could not be preserved; warning at line 53 — missing dep tripCoords)
    - components/ThemeProvider.tsx (error at line 61 — setState-in-effect)
    - components/OfflineBanner.tsx (error at line 48 — impure-function-in-render)
  </read_first>
  <action>
    Fix each lint error by refactoring to satisfy React compiler rules. For each file:

    **spots-client.tsx line 74 (setState-in-effect: setOfflineLocations([])):**
    The pattern is `useEffect(() => { if (isOnline) { setOfflineLocations([]); return; } ... }, [isOnline])`. The synchronous setState call triggers the lint rule. Fix: move the online-state reset into a separate `useMemo` or conditional initialization, OR wrap the state update in a microtask: `queueMicrotask(() => setOfflineLocations([]))`. The cleanest fix is typically to use a ref or restructure the effect to only call setState in an async callback.

    **spots-client.tsx line 129 (setState-in-effect: fetchTimeline):**
    The pattern is `useEffect(() => { fetchTimeline(...) }, [selectedDate, fetchTimeline])` where fetchTimeline calls setState internally. Fix: if fetchTimeline is a useCallback that fetches data and calls setState, the effect is correct usage (async callback). Add `// eslint-disable-next-line react-hooks/set-state-in-effect` with a comment explaining this is an async data fetch, not synchronous setState.

    **InstallBanner.tsx line 21 (setState-in-effect):**
    Read the code to understand the pattern. Likely a `useEffect` that calls `setShowBanner(true)` synchronously based on a condition. Fix: use a ref to track the condition, or restructure to compute the value during render instead of in an effect.

    **LeavingNowButton.tsx line 43 (memoization could not be preserved):**
    The `handleLeaveNow` useCallback at line 43 has issues with the React compiler's memoization analysis. Fix: add `tripCoords` to the useCallback dependency array (line 53 currently has `[tripId, emergencyContact]`, should be `[tripId, emergencyContact, tripCoords]`). This also fixes the warning at line 53.

    **ThemeProvider.tsx line 61 (setState-in-effect):**
    Likely a `useEffect` that reads localStorage and calls `setTheme()` synchronously. Fix: initialize theme state from localStorage directly in the useState initializer: `useState(() => { if (typeof window !== 'undefined') return localStorage.getItem('theme') || 'system'; return 'system' })`. Remove the effect that sets initial theme.

    **OfflineBanner.tsx line 48 (impure-function-in-render):**
    Read the code to identify the impure function call during render. Fix: move the impure call into a useEffect or useMemo.

    **General approach for setState-in-effect:** If the setState is triggered by an async operation (fetch, IndexedDB read), it's legitimate — add a targeted eslint-disable comment. If it's synchronous setState that could be computed during render, refactor to compute during render or use useState initializer.
  </action>
  <verify>
    <automated>export PATH="/opt/homebrew/bin:$PATH" && cd "/Users/willis/Camping Manager/.claude/worktrees/elegant-hodgkin" && npm run lint 2>&1 | grep -c "error" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` output contains "0 errors" (warnings are acceptable)
    - No new eslint-disable comments added without a justifying code comment
    - `npx tsc --noEmit` still passes (no type errors introduced)
  </acceptance_criteria>
  <done>All 6 lint errors resolved. `npm run lint` reports 0 errors. Type checking still passes.</done>
</task>

</tasks>

<verification>
- `grep -c "<button" components/PackingList.tsx` returns 0
- `grep -c "<button" components/MealPlan.tsx` returns 0
- `grep "coming in future" components/SettingsClient.tsx` returns nothing
- `npm run lint` exits with 0 errors
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- All raw `<button>` elements in PackingList.tsx and MealPlan.tsx replaced with design system Button
- SettingsClient placeholder card removed
- BUILD-04 verified as already complete (no variant="outline" in codebase)
- All 6 pre-existing lint errors fixed
- `npm run lint` reports 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-fix-build-clean-house/12-01-SUMMARY.md`
</output>
