---
phase: 13-address-review-findings
plan: 04
type: execute
wave: 2
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - docs/DEFERRED-REVIEW-FINDINGS.md
autonomous: true
requirements: [REVIEW-03]

must_haves:
  truths:
    - "Every LOW finding from the Gemini review is documented with a deferral reason"
    - "Remaining MEDIUM findings not fixed are documented with triage rationale"
    - "npm run build passes after all Wave 1 changes"
  artifacts:
    - path: "docs/DEFERRED-REVIEW-FINDINGS.md"
      provides: "Documented deferrals for LOW and remaining MEDIUM findings"
      contains: "## Deferred"
  key_links: []
---

<objective>
Document deferred findings and verify the build passes after all security fixes.

Purpose: Complete the REVIEW-03 requirement by triaging remaining MEDIUM findings and documenting all LOW findings as deferred to v2.0.
Output: A `docs/DEFERRED-REVIEW-FINDINGS.md` with triage decisions, and a verified passing build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-fix-build-clean-house/GEMINI-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build verification + create deferral document</name>
  <files>docs/DEFERRED-REVIEW-FINDINGS.md</files>
  <read_first>
    - .planning/phases/12-fix-build-clean-house/GEMINI-REVIEW.md
  </read_first>
  <action>
**Step 1: Run `npm run build`** and confirm it passes. If it fails, fix the build errors before proceeding.

**Step 2: Run `npm run lint`** and confirm no new lint errors from the Wave 1 changes.

**Step 3: Create `docs/DEFERRED-REVIEW-FINDINGS.md`** with this content structure:

```markdown
# Deferred Gemini Review Findings

Findings from the Gemini cross-AI review (Phase 12) that were triaged and deferred.
See `.planning/phases/12-fix-build-clean-house/GEMINI-REVIEW.md` for the full review.

## What Was Fixed (Phase 13)

| Severity | Finding | Files | Plan |
|----------|---------|-------|------|
| HIGH | Path traversal in photo import/delete | import/photos, photos/[id] | 13-01 |
| HIGH | XSS in Leaflet popups | SpotMap.tsx | 13-01 |
| HIGH | Unvalidated JSON.parse on LLM output | voice/extract.ts, memory.ts | 13-02 |
| HIGH | Unvalidated JSON.parse in ChatBubble | ChatBubble.tsx | 13-02 |
| MEDIUM | ~13 unvalidated JSON.parse on DB content | Multiple API routes + components | 13-02 |
| MEDIUM | Unsafe parseFloat/parseInt on user input | Gear, trips, vehicle routes + forms | 13-03 |
| MEDIUM | Unsafe date conversion | Trips routes, LocationForm | 13-03 |
| MEDIUM | Weak email validation | Settings route | 13-03 |
| MEDIUM | SSE JSON.parse in chat route | chat/route.ts | 13-02 |
| MEDIUM | JSON.parse in recommend tool | recommend.ts | 13-02 |

## Deferred to v2.0 — MEDIUM

| Finding | File | Reason |
|---------|------|--------|
| Inconsistent parse in makeRunnableTools | app/api/chat/route.ts | Defensive code, not a bug — BetaToolRunner may pass string or object |
| dangerouslySetInnerHTML for theme | app/layout.tsx | Content is hardcoded static script, not dynamic — risk is theoretical |
| JSON.stringify for offline spotId | spots-client.tsx | Performance concern only, not correctness — current dataset is small |
| Unvalidated fetch response for HTML | lib/rag/parsers/web.ts | Cheerio handles malformed HTML gracefully — low risk |
| Unvalidated JSON.parse on voice/apply insights | app/api/voice/apply/route.ts | Input comes from our own extract endpoint, not raw user input |
| Generic offline fallback for API calls | public/sw.js | Working as designed — write queueing exists for critical ops |
| LLM prompt injection risk | lib/claude.ts | Single-user app, no untrusted users — mitigate when multi-user |
| Raw SQL in RAG ingest | lib/rag/ingest.ts | Uses parameterized query (?) — already safe |
| Unvalidated weather API responses | lib/agent/tools/getWeather.ts, weather.ts | External API, type assertion is sufficient for personal tool |

## Deferred to v2.0 — LOW (31 findings)

All LOW findings are deferred. These are optimization and code quality improvements, not correctness or security issues.

**Key themes deferred:**
- N+1 query optimization (trips, chat, dashboard) — not a problem at single-user scale
- Fire-and-forget error monitoring (memory, float plan log) — console.error sufficient for personal tool
- Magic numbers (tile limits, debounce, batch sizes) — functional as-is
- Prisma enum migration for categorical fields — breaking schema change, defer to v2.0
- Missing DB indexes — not a performance concern at current data volumes
- Redundant code patterns (duplicate weather tool, conversation creation) — tech debt, not bugs
- String concatenation for notes — fine at current scale

See GEMINI-REVIEW.md for full details on each LOW finding.
```

Adjust the "What Was Fixed" table based on what was actually fixed in plans 13-01 through 13-03. Verify by checking the SUMMARY files if they exist, otherwise use the plan files.

Note: ChatBubble.tsx already has try-catch around its JSON.parse calls. If 13-02 executor confirms this, move it to "Already handled" rather than "Fixed" in the table.
  </action>
  <verify>
    <automated>cd "/Users/willis/Camping Manager/.claude/worktrees/gracious-tu" && npm run build 2>&1 | tail -5 && test -f docs/DEFERRED-REVIEW-FINDINGS.md && echo "DEFERRED_DOC_EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (no build errors)
    - docs/DEFERRED-REVIEW-FINDINGS.md exists
    - docs/DEFERRED-REVIEW-FINDINGS.md contains `## What Was Fixed`
    - docs/DEFERRED-REVIEW-FINDINGS.md contains `## Deferred to v2.0`
    - Every LOW finding category from GEMINI-REVIEW.md is mentioned in the deferral doc
    - Every MEDIUM finding not fixed is listed with a reason
  </acceptance_criteria>
  <done>Build passes, all findings triaged. Every HIGH/MEDIUM finding is either fixed or documented as deferred with rationale. All 31 LOW findings documented as deferred to v2.0.</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- `npm run lint` passes
- docs/DEFERRED-REVIEW-FINDINGS.md has complete triage of all 74 findings
- Every finding is accounted for: fixed, already handled, or deferred with reason
</verification>

<success_criteria>
- Production build passes with all Phase 13 changes applied
- Complete triage documentation exists for all 74 Gemini review findings
- REVIEW-03 requirement is fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/13-address-review-findings/13-04-SUMMARY.md`
</output>
