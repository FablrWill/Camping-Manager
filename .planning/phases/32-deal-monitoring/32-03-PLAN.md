---
phase: 32-deal-monitoring
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - components/GearDealsTab.tsx
  - components/GearClient.tsx
  - app/gear/page.tsx
autonomous: false
requirements: [HA-03, HA-04, HA-06, HA-07, HA-08, HA-09, HA-10]

must_haves:
  truths:
    - "Deals tab appears in gear modal only for wishlist items"
    - "Deals tab does NOT appear for owned gear items"
    - "Check Price button triggers price check and shows results"
    - "Deal badge (green) shows on wishlist cards when isAtOrBelowTarget is true"
    - "Target price badge shows on wishlist cards when targetPrice is set"
    - "Stale warning appears when price check is over 30 days old"
    - "Staleness disclaimer always visible below price check results"
  artifacts:
    - path: "components/GearDealsTab.tsx"
      provides: "Deals tab component with price check UI"
      min_lines: 80
    - path: "components/GearClient.tsx"
      provides: "Tab extension + deal badges on wishlist cards"
      contains: "'deals'"
    - path: "app/gear/page.tsx"
      provides: "initialDealIds fetched server-side"
      contains: "gearPriceCheck"
  key_links:
    - from: "components/GearDealsTab.tsx"
      to: "/api/gear/[id]/price-check"
      via: "fetch in useEffect + POST on button click"
      pattern: "price-check"
    - from: "components/GearClient.tsx"
      to: "components/GearDealsTab.tsx"
      via: "import and render in modal tab content"
      pattern: "GearDealsTab"
    - from: "app/gear/page.tsx"
      to: "prisma.gearPriceCheck"
      via: "findMany for active deals"
      pattern: "gearPriceCheck"
---

<objective>
Build the GearDealsTab component, integrate it as the third tab in GearClient (wishlist-only), and add deal/target badges to wishlist gear cards.

Purpose: This is the main user-facing UI for deal monitoring — where users check prices and see deal status.
Output: Functional Deals tab in gear modal with price checking, stale warnings, deal badges on cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/32-deal-monitoring/32-CONTEXT.md
@.planning/phases/32-deal-monitoring/32-RESEARCH.md
@.planning/phases/32-deal-monitoring/32-UI-SPEC.md
@.planning/phases/32-deal-monitoring/32-01-SUMMARY.md

@components/GearResearchTab.tsx
@components/GearClient.tsx
@app/gear/page.tsx

<interfaces>
From lib/parse-claude.ts (created in Plan 01):
```typescript
export const GearPriceCheckResultSchema = z.object({
  foundPriceRange: z.string(),
  foundPriceLow: z.coerce.number(),
  foundPriceHigh: z.coerce.number().optional(),
  retailers: z.array(z.string()).default([]),
  disclaimer: z.string(),
});
export type GearPriceCheckResult = z.infer<typeof GearPriceCheckResultSchema>;
```

From app/api/gear/[id]/price-check/route.ts (created in Plan 01):
- GET returns: `{ id, gearItemId, foundPriceRange, foundPriceLow, checkedAt, isAtOrBelowTarget, createdAt }`
- POST returns: same shape after upsert
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GearDealsTab component</name>
  <files>components/GearDealsTab.tsx</files>
  <read_first>
    - components/GearResearchTab.tsx (full file — this is the exact template to mirror)
    - .planning/phases/32-deal-monitoring/32-UI-SPEC.md (copywriting contract, color specs, interaction states)
  </read_first>
  <action>
    Create `components/GearDealsTab.tsx` mirroring `GearResearchTab.tsx` structure exactly. Key differences:

    1. Props interface:
       ```typescript
       interface GearDealsTabProps {
         gearItemId: string
         gearName: string
         targetPrice: number | null
       }
       ```

    2. State: `priceCheck` (fetched data or null), `loading`, `error`, `checking` (for button loading state)

    3. Fetch on mount: `GET /api/gear/${gearItemId}/price-check` — set `priceCheck` on 200, leave null on 404

    4. Staleness: `const STALE_DAYS = 30` (not 90 per D-09). Compute `isStale` same as GearResearchTab: `Date.now() - new Date(priceCheck.checkedAt).getTime() > STALE_DAYS * 24 * 60 * 60 * 1000`

    5. Check Price handler: POST to `/api/gear/${gearItemId}/price-check`, set `checking=true` during, update `priceCheck` on success, set `error` on failure

    6. Layout (per UI-SPEC):
       - If `targetPrice` is null AND no priceCheck exists: show empty state "No target price set" / "Add a target price in the gear edit form to start monitoring deals."
       - If `targetPrice` is set AND no priceCheck exists: show empty state "No price check yet" / "Check current prices using Claude's training data. Prices may be outdated." + "Check Price" button
       - If priceCheck exists:
         - Stale warning banner (only when `isStale`): amber bg, AlertTriangle icon, "Price data is over 30 days old -- consider re-checking."
         - Results card:
           - "Found price range: {foundPriceRange}" — `text-sm font-semibold`
           - "Your target price: ${targetPrice}" (or "Not set" if null) — `text-sm font-semibold`
           - Deal status badge: green `bg-emerald-50 text-emerald-700 dark:bg-emerald-950/30 dark:text-emerald-300` with CheckCircle icon for "Deal!" when `isAtOrBelowTarget`, grey `bg-stone-100 text-stone-600 dark:bg-stone-800 dark:text-stone-400` for "No deal yet"
           - "Last checked: {formatted date}" — `text-xs` secondary color
           - "Re-check Price" button (amber accent, min-h-[44px])
         - Staleness disclaimer (always below results): "Prices based on Claude's training data -- may be outdated." in `text-xs` secondary color

    7. Loading state: RefreshCw icon with `animate-spin` + "Checking prices..." text, button disabled

    8. Error state: error message in `text-red-600 dark:text-red-400` below button, button re-enabled

    9. Icons to import from lucide-react: `Tag, RefreshCw, AlertTriangle, CheckCircle`
  </action>
  <verify>
    <automated>npx tsc --noEmit components/GearDealsTab.tsx 2>&1 || echo "Type check via build"; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/GearDealsTab.tsx exists and is a valid 'use client' component
    - components/GearDealsTab.tsx contains `interface GearDealsTabProps`
    - components/GearDealsTab.tsx contains `const STALE_DAYS = 30`
    - components/GearDealsTab.tsx contains `price-check` fetch URL
    - components/GearDealsTab.tsx contains `"No target price set"`
    - components/GearDealsTab.tsx contains `"No price check yet"`
    - components/GearDealsTab.tsx contains `"Checking prices"`
    - components/GearDealsTab.tsx contains `"Deal!"`
    - components/GearDealsTab.tsx contains `"No deal yet"`
    - components/GearDealsTab.tsx contains `"Prices based on Claude's training data"`
    - components/GearDealsTab.tsx contains `"Price data is over 30 days old"`
    - components/GearDealsTab.tsx contains `"Re-check Price"`
    - components/GearDealsTab.tsx contains `"Check Price"`
    - components/GearDealsTab.tsx contains `bg-emerald-50` (deal badge color)
  </acceptance_criteria>
  <done>GearDealsTab component created with fetch-on-mount, price check button, stale warning (30 days), deal status badges, staleness disclaimer, empty states, loading/error states per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate Deals tab in GearClient + deal badges on cards</name>
  <files>components/GearClient.tsx, app/gear/page.tsx</files>
  <read_first>
    - components/GearClient.tsx (full file — find: activeTab state ~line 79, tab buttons in modal, GearItem local interface ~line 10-31, wishlist card rendering, extraContent pattern)
    - app/gear/page.tsx (full file — find: findMany query, how initialUpgrades or similar data is passed to client)
    - components/GearDealsTab.tsx (just created in Task 1)
  </read_first>
  <action>
    1. In `components/GearClient.tsx`:
       a. Import GearDealsTab: `import GearDealsTab from './GearDealsTab'`
       b. Add `targetPrice: number | null` to the local GearItem interface (alongside existing fields like `price`, `isWishlist`, etc.)
       c. Change activeTab union from `'documents' | 'research'` to `'documents' | 'research' | 'deals'`
       d. Add `initialDealIds` prop to the component: `initialDealIds?: string[]` (set of gearItemIds with active deals)
       e. Create a Set from initialDealIds for O(1) lookup: `const dealIdSet = useMemo(() => new Set(initialDealIds ?? []), [initialDealIds])`
       f. In the modal tab buttons area, add a third tab button for "Deals" — only rendered when `editingItem?.isWishlist === true`:
          ```tsx
          {editingItem?.isWishlist && (
            <button
              onClick={() => setActiveTab('deals')}
              className={/* same pattern as documents/research tab buttons */}
            >
              Deals
            </button>
          )}
          ```
       g. In the modal tab content area, render GearDealsTab when `activeTab === 'deals'`:
          ```tsx
          {activeTab === 'deals' && editingItem && (
            <GearDealsTab
              gearItemId={editingItem.id}
              gearName={editingItem.name}
              targetPrice={editingItem.targetPrice}
            />
          )}
          ```
       h. On wishlist gear cards (in the list rendering), add badges:
          - If `item.targetPrice` is set, show target price badge: Tag icon + `$${item.targetPrice} target` in Badge `default` variant (`bg-stone-100 text-stone-600 dark:bg-stone-800 dark:text-stone-400`)
          - If `dealIdSet.has(item.id)`, show deal badge: CheckCircle icon + "Deal!" in Badge `new` variant (`bg-emerald-50 text-emerald-700 dark:bg-emerald-950/30 dark:text-emerald-300`)
          - Place these right-aligned in a flex row below or beside the item name
       i. Import `Tag, CheckCircle` from lucide-react if not already imported

    2. In `app/gear/page.tsx`:
       a. Add a query to fetch active deal gearItemIds:
          ```typescript
          const activeDeals = await prisma.gearPriceCheck.findMany({
            where: { isAtOrBelowTarget: true },
            select: { gearItemId: true },
          });
          const initialDealIds = activeDeals.map(d => d.gearItemId);
          ```
          Add this to the existing Promise.all if one exists, or as a separate await.
       b. Pass `initialDealIds` prop to `GearClient`:
          ```tsx
          <GearClient initialItems={items} initialDealIds={initialDealIds} />
          ```
       c. Ensure the items serialization includes `targetPrice` in the mapping (add `targetPrice: item.targetPrice` alongside existing fields like `price`, `isWishlist`).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - components/GearClient.tsx contains `'documents' | 'research' | 'deals'`
    - components/GearClient.tsx contains `import GearDealsTab`
    - components/GearClient.tsx contains `targetPrice: number | null` in GearItem interface
    - components/GearClient.tsx contains `initialDealIds` prop
    - components/GearClient.tsx contains `editingItem?.isWishlist` condition for Deals tab button
    - components/GearClient.tsx contains `activeTab === 'deals'` render condition
    - components/GearClient.tsx contains `"Deal!"` badge text
    - app/gear/page.tsx contains `gearPriceCheck.findMany`
    - app/gear/page.tsx contains `initialDealIds`
    - app/gear/page.tsx contains `isAtOrBelowTarget: true`
    - npm run build exits 0
  </acceptance_criteria>
  <done>GearClient has Deals tab (wishlist-only), deal badges on cards, activeTab union extended. Gear page fetches initialDealIds and passes to client. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Deals tab and deal badges</name>
  <files>components/GearDealsTab.tsx, components/GearClient.tsx</files>
  <action>Human verifies the full deal monitoring UI flow in the browser. No code changes — verification only.</action>
  <what-built>GearDealsTab component integrated as third tab in gear modal (wishlist items only). Deal and target price badges on wishlist cards.</what-built>
  <how-to-verify>
    1. Open http://localhost:3000/gear
    2. Open a wishlist item — confirm "Deals" tab appears as third tab
    3. Open an owned item — confirm NO "Deals" tab
    4. In a wishlist item's Deals tab:
       a. If no target price set: see "No target price set" empty state
       b. Edit the item, set a target price (e.g. $100), save
       c. Re-open Deals tab: see "No price check yet" + "Check Price" button
       d. Click "Check Price" — see loading spinner, then results with price range, deal status, disclaimer
    5. Back on gear list: confirm target price badge shows on the card
    6. If price was at or below target: confirm green "Deal!" badge on card
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>User confirms Deals tab, price check, deal badges all work correctly in browser.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
npm run build passes. Deals tab visible for wishlist only. Price check returns results. Badges show on cards.
</verification>

<success_criteria>
- GearDealsTab component renders price check UI with all states (empty, loading, results, stale, error)
- Deals tab only visible for wishlist items in gear modal
- Target price badge and deal badge on wishlist cards
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-deal-monitoring/32-03-SUMMARY.md`
</output>
